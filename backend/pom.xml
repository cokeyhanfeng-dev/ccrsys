<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.ccr</groupId>
    <artifactId>ccr-backend</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>ccr-backend</name>
    <description>客户贡献度与利率决策系统后端(Spring Boot 3 / JDK 17)</description>

    <modules>
        <module>ccr-common</module>
        <module>ccr-application</module>
        <module>ccr-rule</module>
        <module>ccr-approval</module>
        <module>ccr-vote</module>
        <module>ccr-resolution</module>
        <module>ccr-snapshot</module>
        <module>ccr-commitment</module>
        <module>ccr-message</module>
        <module>ccr-workflow</module>
        <module>ccr-admin</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <mybatis-plus.version>3.5.7</mybatis-plus.version>
        <hutool.version>5.8.32</hutool.version>
        <sa-token.version>1.38.0</sa-token.version>
        <warm-flow.version>1.7.4</warm-flow.version>
        <poi.version>5.2.5</poi.version>
        <redisson.version>3.31.0</redisson.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- MyBatis Plus (Spring Boot 3 专用 starter) -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- Hutool 工具集 -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <!-- Sa-Token 认证(Spring Boot 3) -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <!-- 模块依赖 -->
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-application</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-rule</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-approval</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-vote</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-resolution</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-snapshot</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-commitment</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-message</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.ccr</groupId>
                <artifactId>ccr-workflow</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Warm-Flow 工作流引擎(Spring Boot 3) -->
            <dependency>
                <groupId>org.dromara.warm</groupId>
                <artifactId>warm-flow-mybatis-plus-sb3-starter</artifactId>
                <version>${warm-flow.version}</version>
            </dependency>
            <!-- Apache POI(Excel 导入,他行融资明细) -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>
            <!-- Redis 缓存(§3.6;spring-boot-starter-data-redis 版本由 Spring Boot BOM 管理) -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <encoding>UTF-8</encoding>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.34</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
