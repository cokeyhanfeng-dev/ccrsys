# Java / Maven
backend/**/target/
backend/**/*.jar
backend/**/*.jar.original

# Node
frontend/node_modules/
frontend/dist/

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# 日志与临时
*.log
logs/

# 内部临时脚本/工具与认证 token(不入库)
.scratch/

# 本地临时测试构建(不入库):复用 maven 镜像作运行基础,jar 由宿主机预生成
backend/Dockerfile.local

# 项目内隔离工具链与依赖缓存(由 ./dev setup 创建)
.tools/
.cache/

# 部署侧生产连接配置(含真实密码,严禁入库,详设 §3.7)
config/application-prod.yml

# 决议书 PDF 嵌入中文字体(约 20MB,不入库,部署前由本地 fonts 提供)
backend/ccr-approval/src/main/resources/fonts/

# 含真实用户/机构/敏感数据的 SQL:仅本地执行,严禁 push
db/23_password_change_flag.sql
db/24_plaintext_sensitive.sql
db/publish_full.sql
db/prod_full.sql
db/prod_full_v3.0.0.sql

# 增量发布包产物(大文件 jar/dist,不入库;由 scripts/incr-release.sh 生成)
release/
frontend/dist.zip

# 本地 maven 依赖缓存(验证用临时目录,不入库)
.m2cache/
.m2/

# 本地打包/发布中间产物(不入库)
backend.zip
db/prod_full.zip
frontend/src/dist/
