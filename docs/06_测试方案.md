# 客户贡献度与利率决策系统 · 测试方案

> 依据：《详细设计文档》§17、《定稿版 V1.0》§17、《PRD V2》D1-D21
> 适用版本：main（f3bbe77 三轮审查修复 + lane-01/02 并入 + lane-05/06 待合并）
> 日期：2026-08-07

---

## 1. 范围与对象

| 在内 | PC 管理端全部功能（申请/审批/表决/行长决策/决议/承诺/配置/系统管理/审计/数据中心）、后端 API、定时任务、数据库 |
|---|---|
| 不在内 | 移动端 H5（未建设）、与数仓/信贷核心的真实联调（当前 mock 数据）、Redis 缓存（lane-01 已并入代码，需在部署环境验证） |

## 2. 测试环境

```bash
cd G:/project/ccr
docker compose down -v && docker compose up -d --build   # 改种子/DDL 必须 -v
```

| 组件 | 地址 |
|---|---|
| 前端 | http://localhost:3000 |
| 后端 | http://localhost:8080（/health 健康检查） |
| MySQL | localhost:13306（ccr_rate / root / root123） |

### 2.1 测试账号（密码均 123456）

| 账号 | 角色 | 用途 |
|---|---|---|
| zhangsan / lisi | 客户经理（城东/宜城支行） | 申请、数据权限 SELF 隔离对照 |
| wangwu | 支行行长 | 必经首节点审批 |
| deptgm / vicepresident | 部门总经理 / 分管行领导 | 逐级上送审批（分项 dept_code 为空时的兜底账号） |
| deptgm_sxsb / deptgm_lsb | 授信评审部/零售金融部总经理 | 分项 dept_code=100102/100103 时 GM 节点处理人（§D16a 部门分流） |
| vicepresident_gsb / vicepresident_sxsb | 分管行长（公司金融部/授信评审部分管） | 分项 dept_code=100101/100102 时 VP 节点处理人（ccr_dept_vp 部门映射，§12.2） |
| committee ~ committee6 | 审批小组成员 ×6 | 匿名表决 |
| president | 总行行长 | 最终决策 |
| contractor | 合同经办岗 | 决议合同回填、执行核验 |
| reviewer | 配置复核人 | 参数复核发布 |
| auditor | 审计人员 | 投票人反查、导出 |
| admin | 系统管理员 | 系统/参数/流程配置 |

### 2.2 演示数据（db/10_mock_external.sql）

| 数据 | 值 |
|---|---|
| 单户客户 | CUST001（对公）、存款演示账号 `9550880000000101`（余额 1200 万、利率 1.25%） |
| 集团 | GROUP001（批复 1 亿），成员 MEMBER_A（6000 万）/MEMBER_B（4000 万），账号 `9550880000000001/2` |
| LPR 生效版 | 1Y=3.0%、5Y+=3.5% |
| 权限矩阵 | 75 行 EFFECTIVE（PRD §7.2 全量） |
| 核验异常演示 | 借据 NB_A003 利率 3.9% ≠ 合同 3.6% |

## 3. 测试分层

| 层级 | 现状 | 本方案增量 |
|---|---|---|
| 单元测试 | 221 例全绿（规则 30/表决 41/审批 23/申请 35/快照 19/决议 15/承诺+消息 12/公共 46） | 随缺陷修复补充 |
| 接口测试 | 无 | Postman/MockMvc 集合（§5 用例的 API 化） |
| 端到端（手工+脚本） | 冒烟通过 | §4 主链路用例全量执行 |
| UAT | 无 | §7 验收清单逐项签字 |

## 4. 功能测试用例（端到端主链路）

> 每条用例给出：前置数据 → 操作（角色@页面）→ 预期（含状态/数据断言）。执行顺序即业务流程顺序。

### T1 路由引擎与权限矩阵（核心，先于流程用例执行）

口径：贷款必经支行行长首节点；非国企新增<5000万与个人新增中支行"权限内"终审边界=该单元格部门总经理线（挂牌价）；突破逐级上送 GM→VP→小组；存款/保证金支行过手后直接上会。

| 编号 | 输入（业务/新旧/客户类型/金额/期限/申请利率） | 预期终审 |
|---|---|---|
| T1-01 | 对公/新增/非国企/3000万/1Y/3.5%（=LPR+50BP≥GM线3.4） | 支行行长 |
| T1-02 | 同上 /3.3%（[LPR+20, LPR+40)） | 分管行领导（链：支行→GM→VP） |
| T1-03 | 同上 /2.9%（<LPR） | 六人小组 |
| T1-04 | 对公/新增/国企/3000万/1Y/3.1%（≥LPR） | 部门总经理（支行无权限仅过手） |
| T1-05 | 对公/新增/国企/3000万/1Y/2.95%（[LPR-10,LPR)） | 分管行领导 |
| T1-06 | 对公/新增/国企/6000万/1Y/3.1% | 部门总经理（5000万以上按矩阵，不强制上会——已拍板） |
| T1-07 | 金额恰 5000 万整 | 归 GE_5000 高档（已拍板） |
| T1-08 | 对公/存量/国企/3000万/原利率 4.0→申请 3.8（降幅 20BP≤30 且≥3%） | 部门总经理（公司金融部） |
| T1-09 | 同上 4.0→3.6（降幅 40BP>30 且≥3%） | 分管行领导 |
| T1-10 | 同上 4.0→2.9（<3% 上会小组分界,≥2.8% 硬边界） | 六人小组表决（低于 2.8% 硬边界才阻断） |
| T1-11 | 个人经营贷/新增/1Y/4.1%（≥LPR+100） | 支行行长（零售金融部链） |
| T1-12 | 个人经营贷/新增/1Y/3.9%（[LPR+80,+100)） | 分管行领导 |
| T1-13 | 存款/对公定期/1Y/1.30%（>1.25% 上限） | 支行过手→六人小组 |
| T1-14 | 存款/对公定期/3M/0.80%（≤0.85% 上限） | 不超上限，支行行长权限内终审 |
| T1-15 | 集团 GROUP001 申请（分项 4000 万） | 金额档按集团批复总额 1 亿=GE_5000（B18 防拆分规避） |
| T1-16 | LPR 发布后（如 1Y 调 2.9%）重算 T1-02 | 边界自动换算（3.1%），在途申请仍按冻结版本 |

验证方式：`POST /ccr/rule/matrix-route` 逐条断言 `startNodeCode/finalNodeCode/routeChain/boundaryRate/matchedMatrixNo`；前端申请页"路由预览"同步核对。

### T2 申请与提交

| 编号 | 场景 | 预期 |
|---|---|---|
| T2-01 | zhangsan 创建贷款草稿→存草稿→再编辑保存→重新载入 | 子表（成员/分项/担保/承诺）随 PUT 全量重建且内容最新；versionNo 冲突返回 1010 |
| T2-02 | 贷款申请：选客户 CUST001 → 填分项 → 路由预览 | 逐分项展示链路/终审/边界/LPR 版本 |
| T2-03 | 提交（submit-check 有 WARN 无 BLOCK）→ 差异确认弹窗 → 确认提交 | 快照冻结绑定、lpr/规则版本冻结、分项 ROUTING+BRANCH_MANAGER、主单 DRAFT→SUBMITTED→ROUTING、Outbox 写 FLOW_START/NOTIFY |
| T2-04 | 重复点击提交 / 重复调 submit | 幂等返回已有结果，不产生重复任务 |
| T2-05 | 数据时效：将某 dw 表 data_dt 改 4 天前 | submit-check 与 submit 均 BLOCK（提示"数据源数据日期过期，请联系数据中心刷新"） |
| T2-06 | 贡献度勾稽：把 CUST001 的 TOTAL 行改偏 | 质量校验 BLOCK（差额超容差） |
| T2-07 | 非信用类分项不录担保措施 | 提交阻断（§9.3-5） |
| T2-08 | 集团申请：成员金额合计超集团批复总额 | 阻断（LIMIT_INCONSISTENT）；SHARED 组不简单加总 |
| T2-09 | 同一合同在两个在途申请中各建分项 | 第二个提交被阻断（一合同一有效分项） |
| T2-10 | 存款申请：输账号 9550880000000101 失焦 | 自动带出产品/期限/余额/当前利率；乱输账号提示未命中但不阻断；拟开户无需账号 |

### T3 普通节点审批

| 编号 | 场景 | 预期 |
|---|---|---|
| T3-01 | wangwu 看待办 | 仅本机构（城东支行）申请；lisi（宜城）申请不可见 |
| T3-02 | 用 zhangsan（客户经理）调 approve 接口 | 拒绝（节点权限不足） |
| T3-03 | 篡改请求体 nodeCode/传他人 operatorId | 无效：身份取登录态，nodeCode 必须=分项当前节点 |
| T3-04 | 审批详情 | 区块齐全且 source=SNAPSHOT（冻结快照徽标+数据日期）；存款显示账户卡+上限比对 |
| T3-05 | 支行行长调价到权限内并通过 | 终审 APPROVED_LEVEL→自动签决议；调价记录落 ccr_rate_adjustment（含前后值/边界/操作人） |
| T3-06 | 调价突破本节点权限边界 | 拒绝（NODE_PERMISSION）；突破产品硬边界拒绝（HARD_BOUNDARY） |
| T3-07 | 保留超权限利率并通过 | 自动上送下一级，分项仍 ROUTING、节点推进 |
| T3-08 | 否决不填原因 | 拒绝（必填） |
| T3-09 | 同一分项双击通过/两人同时审批 | 仅一次生效（TASK_PROCESSED/版本冲突） |

### T4 六人匿名表决

| 编号 | 场景 | 预期 |
|---|---|---|
| T4-01 | 超权限上送小组 | 自动建批、6 人名单冻结、分项 VOTING |
| T4-02 | committee1 登录表决 | 只见本人任务+本人票型；无他人票/进度/汇总 |
| T4-03 | 同一委员重复投票 | 唯一约束拦截（DUPLICATE_VOTE） |
| T4-04 | 冒充他人投票（传 voterUserId） | 无效（身份取登录态，非委员拒绝） |
| T4-05 | 6 人投 4 赞成 2 否决 | 满 6 票自动计票 PASS→PRESIDENT_DECISION；批次在全分项计票完成后才关闭 |
| T4-06 | 3 赞成 3 否决 | FAIL→分项 REJECTED 终态 |
| T4-07 | 多分项批次：第一个分项投满 6 票 | 批次不提前关闭，其余分项可继续投（回归此前 bug） |
| T4-08 | 批次超 72h 未投齐 | 超时任务按已投票数计票（赞成≥4 通过） |
| T4-09 | 委员替补（president/admin 发起，录原因） | 原成员 REPLACED、已投票保留、替补投剩余项 |
| T4-10 | 普通角色调 GET /ccr/vote-results/{id} | 403；president/auditor 可见 |

### T4A 六人小组整单改造（V3.4，委员内联审批 + 整单否决）

| 编号 | 场景 | 预期 |
|---|---|---|
| T4A-01 | 委员待办多分项同申请 | 按 applicationId 聚合成一张申请卡（卡内展开分项，标题为申请号），不再按分项拆卡 |
| T4A-02 | 委员进入审批详情 | 页面与支行行长/分管行长审批页**一致**（复用 detail.vue 审批决定卡片）；**无"审批后利率"输入框**（只读展示审批后利率） |
| T4A-03 | 委员页分项按钮 | 仅小组 VOTING 分项显示"同意/否决"；非表决范围分项标注"仅展示"、无按钮 |
| T4A-04 | 委员一键否决 | 弹"整单否决"确认框；确认后对全部待表决分项提交否决票，提示"等待其他委员投票" |
| T4A-05 | 小组否决（3 否决/3 赞成） | 触发分项 REJECTED；**同申请不需小组表决、仍在途的分项一并 REJECTED**（整单否决） |
| T4A-06 | 小组否决出决议书 | 签发否决决议（decisionSource=COMMITTEE_REJECT），PDF 结论"否决（小组表决否决）"；**不建承诺计划** |
| T4A-07 | 工作台委员待办 | 按申请聚合展示；统计卡"待我表决"入口指向 `/approval`（原 `/voting` 废弃） |
| T4A-08 | 行长查看小组结果 | 仅匿名码 A–F + 票型（同意/否决）+ 汇总票数，不见成员身份 |

### T5 行长决策

| 编号 | 场景 | 预期 |
|---|---|---|
| T5-01 | president 待决策列表 | 仅小组通过的分项；详情含汇总票数/匿名意见折叠块（委员A-F） |
| T5-02 | 同意利率 | 分项 FINAL、finalRate 回填、决议自动签发（ISSUED）、承诺计划自动创建（经 Outbox） |
| T5-03 | 一票否决不填意见 | 拒绝；填意见→VETOED 终态 |
| T5-04 | 行长尝试修改利率 | 无此能力（接口无调价参数） |

### T6 决议、合同回填与核验

| 编号 | 场景 | 预期 |
|---|---|---|
| T6-01 | 决议生成后 | 状态 ISSUED + 执行记录 CONTRACT_PENDING；决议号 RES+日期+序列；一分项一决议（幂等） |
| T6-02 | 合同回填：金额/期限小于决议 | 拒绝（≥ 校验）；超决议有效期拒绝；七项要素不符拒绝并列出 |
| T6-03 | 同一合同绑第二个未关闭决议 | 拒绝 |
| T6-04 | 回填成功 | 自动两级核验：利率一致→EXECUTED |
| T6-05 | 借据 NB_A003（3.9%≠合同 3.6%） | RECONCILE_EXCEPTION + difference_json 明细 + 通知（客户经理+合同经办岗，message_key 幂等不重复） |
| T6-06 | 异常不篡改决议 | ccr_resolution 行无变化 |
| T6-07 | 普通登录角色直接 POST `/ccr/resolutions` | 405（外部决议创建方法已移除，签发仅由 Outbox 串联） |
| T6-08 | 非合同经办岗/行长/admin 调合同回填或执行核验 | 403；contractor 可进入且仍需通过七项校验 |

### T6A 对象授权与敏感接口

| 编号 | 场景 | 预期 |
|---|---|---|
| T6A-01 | 客户经理读取、保存、预览、校验、提交或重提他人申请 ID | 403；本人申请正常 |
| T6A-02 | 支行行长访问其他支行申请详情或附件 | 403；所属支行及下辖网点申请正常 |
| T6A-03 | 部门总经理/分管行长访问未指派且未历史经办申请 | 403；本人当前任务或历史经办正常 |
| T6A-04 | 委员访问未分配表决批次申请 | 403；本人有效分配批次正常 |
| T6A-05 | 非 admin 访问数据中心、快照技术接口 | 403 |
| T6A-06 | 非客户经理访问客户/集团/成员数仓查询或他行融资导入 | 403 |
| T6A-07 | 关联人绑定缺 applicationId、伪造 customerNo/groupNo 或引用他人/非草稿申请 | 400/403/1011；本人草稿绑定正常 |
| T6A-08 | 节点指派配置查询故障 | 500 并拒绝按角色兜底；确实无配置时仍按节点角色兜底 |
| T6A-09 | 非 admin 调手工通知、重试、承诺计划创建/手工评估 | 403 |
| T6A-10 | config_reviewer 调配置维护/送审/停用 | 403；查看、发布、驳回正常；admin 可维护 |
| T6A-11 | 跟踪策略跳过状态、维护人自行发布或同维度重复生效 | 非法迁移 1010；自行发布 403；新策略生效后旧策略自动停用 |
| T6A-12 | 城东/宜城客户经理提交申请 | 仅对应 `branch_code` 的支行行长收到通知；解析不到接收人时事件重试并终态告警 |

### T7 否决与关联重提（D18b/D14）

| 编号 | 场景 | 预期 |
|---|---|---|
| T7-01 | 分项被否（普通节点/表决/行长一票）后整单终态 | 主申请 REJECTED（全否）或 FINAL（混合）；原分项终态不可回退 |
| T7-02 | 历史申请点"关联重提" | 新申请号、复制客户/成员/担保/承诺/说明；已批准分项 inheritFlag=Y 沿用原决议不重审；被否/新增分项重新路由 |
| T7-03 | 重提后原申请 | 状态置 RETURNED |
| T7-04 | 重提草稿编辑后保存 | 继承分项不被删除（回归此前数据丢失 bug） |
| T7-05 | 重提存款申请 | 仍走支行行长必经等现行路由 |

### T8 承诺跟踪与消息

| 编号 | 场景 | 预期 |
|---|---|---|
| T8-01 | 决议签发后 | 按申请承诺自动生成计划（PENDING→TRACKING），策略版本冻结 |
| T8-02 | 日常评估任务（或手工 evaluate） | 实际值取数仓最近批次当前贡献度（D21）；评估记录不覆盖历史（含 calc_version） |
| T8-03 | 数据超容忍天数 | DATA_PENDING，不误判未履约 |
| T8-04 | 达成率 < 阈值 | AT_RISK + 预警通知（接收人按规则动态解析；同计划同周期只发一条） |
| T8-05 | 到期未达成 | EXPIRED_UNMET + 提醒支行行长与部门（dept_gm） |
| T8-06 | 集团 FIXED_ALLOCATION 成员合计≠集团目标 | 建计划拒绝 |
| T8-07 | 贡献度跟踪页 | 三级钻取（客户→承诺记录→指标进度条/评估历史/月报）；数据权限：客户经理仅本人、zhangsan 与 lisi 互不可见 |

### T9 参数配置（admin/reviewer）

| 编号 | 场景 | 预期 |
|---|---|---|
| T9-01 | LPR 新增草稿→送审→发布 | 发布生效；0.5-8% 区间/0.05 步长/同日一版/生效日≥发布日校验 |
| T9-02 | 同一人建草稿并发布 | 拒绝（双人复核 createBy≠publishBy） |
| T9-03 | 复核驳回（填意见） | 状态回 DRAFT，意见留痕 |
| T9-04 | 发布新 LPR 后 | 旧版自动停用；新路由按新值换算；在途申请按冻结版本不变 |
| T9-05 | 矩阵行发布：边界互斥缺失/区间交叠/低于产品硬边界 | 拒绝并给出原因 |
| T9-06 | 变更日志 | create/submit/publish/disable/reject 均落 ccr_config_change_log，审计可查 |
| T9-07 | 产品边界维护 | product-limit 全流程可维护并即生效于硬边界校验 |
| T9-08 | 矩阵路由试算 | 输入维度输出终审岗位+链路，与真实提交路由一致 |

### T10 权限与数据权限

| 编号 | 场景 | 预期 |
|---|---|---|
| T10-01 | 各角色登录 | 菜单按角色过滤；直接输 URL 越权跳 /overview 并提示 |
| T10-02 | zhangsan 调 /system/users | 403（仅 admin） |
| T10-03 | reviewer 访问参数管理 | 可用；zhangsan 403 |
| T10-04 | 承诺列表传 roleCode=admin | 无效（服务端取登录态） |
| T10-05 | zhangsan 与 lisi 互查申请列表 | 仅见本人（SELF）；wangwu 见本支行及网点（DEPT 前缀） |
| T10-06 | 节点指派（admin 流程配置页） | 按人/角色/部门指派+代理+解析预览；有指派时待办收窄到指派人，无指派角色兜底 |

### T11 审计与导出

| 编号 | 场景 | 预期 |
|---|---|---|
| T11-01 | auditor 投票人反查 | 返回真实投票人+票型+匿名码对照；查询留痕 ccr_audit_log |
| T11-02 | 档案导出 xlsx | 14 个 sheet、水印（操作人/机构/时间）、写 ccr_export_record；非 admin/auditor/president 无按钮且接口 403 |
| T11-03 | 历史档案页 | §14.4 全区块（含快照日期/质量/轨迹/表决汇总/决议/核验/承诺） |

### T12 系统管理与数据中心

| 编号 | 场景 | 预期 |
|---|---|---|
| T12-01 | 机构 CRUD/启停用 | 编码前缀规则、成环校验、存量业务（在途申请/任务/承诺）阻断停用并报笔数 |
| T12-02 | 用户多机构岗位绑定 | 多行保存、恰一条默认 |
| T12-03 | 数据中心页 | 批次监控+时效看板；把某表 data_dt 改旧→STALE 红灯 |

## 5. 非功能测试

| 项 | 方法 | 目标 |
|---|---|---|
| 并发投票 | 6 委员并发脚本 POST ballots | 一人一票、计票恰好一次 |
| 并发审批 | 双浏览器同分项同时 approve | 一人成功一人 TASK_PROCESSED |
| 重复提交 | 双击/重放 submit、带幂等键重放 | 不产生重复分项/任务/决议 |
| 性能 | 审批详情/列表 P95 | ≤2s；提交+快照冻结 ≤5s |
| 安全 | 越权/伪造身份/敏感字段 | 接口 403；密文不落明文；日志无证件明文 |
| 定时任务 | 手动触发评估/月报/超时扫描/Outbox 消费 | 幂等可重跑、失败重试退避 |

## 6. 自动化现状与缺口

- **已有**：221 个单测覆盖路由矩阵/计票/决议/快照/承诺/Outbox/数据权限/指派解析（CI 已配 .github/workflows/ci.yml，push 即跑）
- **缺口**：①接口级契约测试（建议 MockMvc 或 Postman 集合，把 T3/T4 安全用例自动化）；②前端 E2E（Playwright 主链路 1 条：申请→支行审批→通过）；③集成测试目前用 Mockito，无 Testcontainers 真库测试；④ccr-admin 的 OutboxHandlersTest 在合并 worktree 中超时挂起，需排查（疑似重试退避真实休眠）
- **建议**：把 T1 路由 16 条做成参数化单测直接挂在 RateMatrixRouterImplTest（已部分存在）；T3-02/03、T4-03/04 安全用例补接口测试

## 7. UAT 验收清单（对照 PRD 决策）

| 决策 | 验收点 | 对应用例 |
|---|---|---|
| D1 | LPR 参数化，在途申请用冻结版本 | T1-16、T9-04 |
| D3 | 支行行长权限内终审/超挂牌价上送 | T1-01/02、T3-05/07 |
| D5/D9 | 六人匿名、≥4 票、无 2/3 措辞 | T4 全组 |
| D6/B18 | 集团按批复总额度定档 | T1-15 |
| D10 | 移动端仅审批（本期无移动端，PC 无混入） | — |
| D11 | 审批读冻结快照、无刷新按钮 | T3-04 |
| D12 | LPR 人工维护+复核发布 | T9-01/02 |
| D13 | 贡献度勾稽 | T2-06 |
| D14 | 重提继承补录 | T7-02 |
| D15 | 拟达成不勾稽不设时效 | 设计评审+T2 观察 |
| D16b | 存款直接上会（支行必经后） | T1-13 |
| D18 | 逐担保类型独立；已批准沿用 | T7-02、T4-07 |
| D19 | 机构达成率展示 | T3-04（机构达成卡） |
| D20 | 小组通过必经行长 | T4-05→T5-01 |
| D21 | 承诺以数仓当前贡献度为实际值 | T8-02 |

## 8. 执行计划与出口准则

| 阶段 | 内容 | 出口准则 |
|---|---|---|
| S1 冒烟 | 健康检查/登录/路由试算/集团接口 | 全通过（当前已过） |
| S2 主链路 | T1-T7 顺序执行 | 无 🔴 缺陷；三主体各 1 条全链路走通 |
| S3 扩展 | T8-T12 + 非功能 | 无 🔴/🟡 遗留 |
| S4 UAT | §7 清单业务签字 | D1-D21 逐项确认 |

**缺陷分级**：🔴 主链路阻断/数据错误/安全越权；🟡 功能不完整或口径偏差；🔵 体验/边缘问题。🔴 不过夜，🟡 本期闭环，🔵 登记排期。

## 9. 已知遗留（测试时豁免或关注）

1. ccr-admin OutboxHandlersTest 在 lane 合并 worktree 中超时挂起（主分支通过）——合入 lane-05/06 前需先定位
2. 数据权限统一按领域服务对象授权与服务端列表过滤验证；已删除零使用的 `@DataScope` SQL 改写链，T10-05 的 DEPT 级以 `apply_branch_code` 和对象关系断言为准
3. 数仓数据为 mock；真实回填后需回归 T2-05/06、T8-02
4. 决议有效期 180 天/表决超时 72h 等为配置项，测试可用小值加速

---

## 10. 修订（2026-08-10，实现对齐版）

以下用例按最新实现修订/新增，与上文冲突处以本节为准：

### 10.1 修订项

| 原用例 | 修订后预期 |
|---|---|
| T2-05 数据时效 | 阈值=3 个自然日（非 7 天），超期 BLOCK；mock 日期用 `db/15_test_data.sql` 刷新（CURDATE()） |
| T3-01 wangwu 待办 | **仅城东支行（含网点）客户经理的申请**；宜城支行（lisi）的申请不可见也不可审（§5.4 前缀过滤） |
| T3-04 审批详情区块 | 更新为：醒目利率决策区/申请内容/客户信息/授信与本行融资/**他行融资**/**关联人**/合同与担保（措施按类型完整字段）/存款账户卡（存款）/贡献度（仅贷款）/历史履约/机构达成/**材料附件**/流程轨迹/审批操作。**无流程路由卡、无资料校验卡** |
| T9-07 参数配置 | 已实现域：LPR/权限矩阵/规则集/产品边界+变更日志；数据集/字典等 5 域为二期，不测 |
| T12-03 数据中心 | 仅 admin 可见（非全角色） |

### 10.2 新增用例

| 编号 | 场景 | 预期 |
|---|---|---|
| N1 分步校验 | 向导每步点"下一步"/跳步 | 当前步骤必填缺失时提示具体字段（如"第 2 条分项未录入申请利率"） |
| N2 申请机构 | 申请页机构字段 | 显示客户经理所在支行名（只读），如"城东支行" |
| N3 跨支行隔离 | lisi 提交申请 → wangwu 待办 | 不可见；直接调 approve 接口也被拒"非本支行客户经理的申请" |
| N4 附件全链路 | 材料附件步骤选文件 | 草稿态即上传（徽标"已上传"）；审批详情"申请材料附件"卡可下载 |
| N5 担保差异化 | 担保类型切 质押/保证金/存单质押；抵押类型切 住宅/土地/设备/车辆 | 各自专属字段；详情按类型完整回显 |
| N6 拟签合同 | 新增授信不填拟签合同标识 | 后端自动生成 PLANNED-分项号，可正常提交 |
| N7 会话过期 | token 过期后操作 | 任意接口返回业务码 401 → 自动跳登录页（带 redirect） |
| N8 流程定义查看 | 流程配置→流程定义→查看 | 弹窗渲染节点+跳转流程图（中文节点名） |
| N9 工作台 | 各角色登录工作台 | 角色差异化总览（审批角色：待我审批/已办/跟踪概况；客户经理：我的申请动态）；与利率审批页不重复 |
| N10 履约评估手工触发 | POST /ccr/commitments/plans/{id}/evaluate，body `{"dataDt":"当日"}` | 生成评估记录；审批详情历史履约显示达成率 |
| N11 存款场景精简 | 存款申请页与审批详情 | 无关联人员、无贡献度录入/参考区块 |
| N12 长 ID | 建草稿后提交 | 雪花 ID 字符串传输，无"申请不存在"精度问题 |

---

## 11. 最新调整专项（2026-08-12，覆盖 #73/#74/#76/#42 + 手册 #72）

> ⚠️ 2026-08-13：本节 L1-L13 专项用例已**整合入重写版 `docs/07_手工测试用例.md` 场景 M（M1-M5）**，执行测试以 07 为准；本节保留策略口径与数据准备说明。
> 数据准备：新增 `db/17_test_data_latest.sql`（幂等，已灌入运行库）。与前文冲突处以本节为准。

### 11.1 修订项

| 原用例 | 修订后预期 |
|---|---|
| T2-02 申请要素区 | **无「贷款品种」「金额档」字段**（已删除），仅保留「业务类型」；存量/新增切换正常（#73） |
| T10-05 / N3 数据权限 | 前缀匹配基础已由 `ccr_sys_dept.org_code` + `sys_org.branch_code` 双重冗余；改机构编码需同步两表 + 回刷存量 `ccr_application.apply_branch_code`（详见 §11.4 回归） |
| T12-03 数据中心 | 表清单**动态化**（#42）：`GET /ccr/datacenter/batches` 返回 19 张（含 A11 `dw_credit_agreement_snapshot`、**不含弃用 `dw_org_dim`**）；新增/删除 dw_/caps_ 表后无需改代码自动纳入/剔除 |

### 11.2 新增用例——授信信息手工补录（#74）

| 编号 | 场景 | 预期 |
|---|---|---|
| L1 新增授信补录 | zhangsan 选 CUST004（数仓无数仓协议）→ 业务类型=新增 → 授信信息区**全空** | 可手工补录完整协议要素：授信类型/币种/状态/授信额度/已用/可用/起止；**协议号可空（placeholder「新增可空(拟签)」）**；总授信额度可编辑 |
| L2 新增补录提交 | 补录后提交（协议号留空）→ 审批详情授信信息区 | 补录行 `source=APPLICATION` 置顶显示，协议号显示「—」；badge「申请补录 + 数仓」；`ccr_application.credit_info_json` 落库 JSON |
| L3 存量可修正 | 选 CUST001 存量调息 → 协议 AGR-C001-2026B 带出 → 修改授信额度 | 要素可编辑；修正值随提交覆盖展示，审批详情补录优先于数仓 |
| L4 补录去重 | 补录协议号与数仓已有协议号相同 | 合并去重（补录行覆盖数仓行，不产生重复条目） |
| L5 未补录新增 | CUST004 新增不补录任何授信信息直接提交 | 不报错；`credit_info_json` 为空不落库；审批详情仍显示「暂无授信协议数据」（数仓兜底） |

### 11.3 新增用例——担保方式收敛 4 种 + 数据中心动态化（#76/#42）

| 编号 | 场景 | 预期 |
|---|---|---|
| L6 担保下拉收敛 | 申请分项担保方式下拉 | **仅 4 种：抵押/质押/保证/信用**；银票/信用证保证金、存单质押、保证金质押**不再出现为选项** |
| L7 存量担保中文回显 | 审批详情/历史档案看 CCR202608114AE7 的担保措施 | 存量 `BILL_MARGIN` 措施按 `GUARANTEE_NAME_MAP` 中文显示「银票保证金」，不因收敛丢失（数据来自 17_test_data §3） |
| L8 数据中心动态清单 | `GET /ccr/datacenter/batches` 与 `/source-status` | 19 行：含 `dw_credit_agreement_snapshot`、不含 `dw_org_dim`；各表 `sourceName` 中文、未知表回退表名；status 按 `delayDays > 3` 判 STALE |
| L9 动态新增表 | 建临时表 `dw_temp_verify` → 重新调 batches 接口 → 删表 | 接口自动出现该表（第 20 行，说明回退表名）；删表后自动消失 |
| L10 前端字段对齐 | 数据中心页渲染 | 批次表列=数据表/数据源/最新数据日期/批次行数/落地时间；时效看板按 table 展示 OK/STALE（不再有 batchNo/costMs 失配） |

### 11.4 新增用例——机构编码调整回归（对照手册 §2/§5.4 与用户确认的编码规则）

> 背景：编码规则「1000 总行/1001xx 部门/1002xx 支行/支行码+两位网点」仅约束层级前缀包含关系，
> 代码无硬编码数字；数据权限实际走 `branch_code` 独立字段。调整 org_code 的三条硬性前提：
> ①父编码必须是子编码前缀；②`ccr_sys_dept` + `sys_org` 两表同步；③回刷存量 `apply_branch_code`。

| 编号 | 场景 | 预期 |
|---|---|---|
| L11 前缀隔离回归 | 城东支行行长 wangwu 待办 | 仅城东支行（100201）及其网点（10020101）客户经理的申请；宜城（100203/lisi）不可见（对照 §11.1 T10-05） |
| L12 编码调整演练 | 在**临时库**将支行 org_code 改编码段（如 100201→200201）并同步两表 + 回刷存量后 | 前缀过滤随之变化；改后 wangwu 以新 branch_code 仍只看本支行；旧 apply_branch_code 若未回刷则历史申请不可见（验证回刷必要性） |
| L13 手册配置验证 | 按 `docs/10` 操作手册调整：节点指派四层/部门分管行长/利率矩阵边界 | 调整后审批待办收窄到指派人、分管行长一人分管多部门生效、矩阵边界生效（对应手册第 3/4/5 章样例） |

### 11.5 数据准备汇总

- `db/15_test_data.sql`：存量（CUST001/002/101、集团、借据、贡献度、授信协议 10 条）——基线不变。
- `db/17_test_data_latest.sql`（新增）：
  - **CUST004**：数仓客户主数据 + 征信，**无数仓授信协议**（供 L1/L2/L5 新增授信补录）；
  - **AGR-C001-2026C**：CUST001 第三份循环授信（已用接近额度，供 L3 存量修正核对）；
  - **MS-BILL-001**：挂 CCR202608114AE7（FINAL）担保组合的 BILL_MARGIN 存量措施（供 L7 中文回显）；
  - 数据中心 19 张表 data_dt 刷新（CURDATE()，防时效阻断）。
- 临时表 L9 验证用：`CREATE TABLE dw_temp_verify ...; DROP TABLE dw_temp_verify;`（不入脚本，测试时手动）。

## 12. 手工测试造数专项（2026-08-12，用户反馈 #1/#2/#4/#5/#6 回归 + 全量清理重造）

> ⚠️ 2026-08-13：本节 S1-S5 造数场景与用户反馈回归断言已**整合入重写版 `docs/07_手工测试用例.md` 场景 B2 与场景 R（R1/R2/R3）**，执行测试以 07 为准；本节保留造数记录与配置口径。

### 12.1 背景与前置配置调整

**数据清理**：`db/20_clean_biz.sql`（幂等，2026-08-13 已增强：TRUNCATE 全部业务表——申请/分项/审批动作/表决/决议/承诺/快照/通知/审计/导出/outbox/变更日志 + **Warm-Flow 运行表 flow_instance/flow_task/flow_his_task/flow_skip**；保留系统主数据、配置表、数仓 mock、流程定义 flow_definition/node/user）。执行：`docker exec -i ccr-mysql mysql -uroot -proot123 --default-character-set=utf8mb4 ccr_rate < db/20_clean_biz.sql`。

**配置调整**（测试环境运行库，非 seed 脚本，需随造数一起应用）：
1. `ccr_product_route LOAN_A`：`mandatory_vote` Y→N。原配置无条件强制上会，使矩阵分层（支行/GM/VP 终审）对 LOAN_A 全部失效；改为保留 `vote_condition`（`{"amount_tier":"GE_5000","enterprise_type":"SOE"}`）命中才强制上会，恢复权限内分层终审。**vote_condition 为 AND 语义（金额档与企业类型同时命中才上会，`RateMatrixRouterImpl.voteConditionHit`，2026-08-12 修复）**——仅 `enterprise_type=SOE` 命中不触发，避免国企分层（GM/VP 终审）被全部拉上会。
2. `ccr_rate_matrix M-PUB-EX-GE-NSOE-VP`：`boundary_bp` 20→70（SPREAD 边界 3.8%→3.3%）。原 VP 边界（3.8%）高于支行节点权限下限（3.4%），导致对公存量≥5000万非国企的 VP 终审行永远被支行权限内截胡；调整后 rate 3.3%（降幅 70bp）可触发"支行超权限上送→沿冻结链直达 VP（跳 GM）"，回归 #2/#4。

应用后清理路由/产品链路缓存：`docker exec ccr-redis redis-cli DEL ccr:cfg:matrix:effective ccr:cfg:product-route:effective`。

**造数脚本**（API 真实流程，不入库）：`ccr-seed/seed_data.py`（登录→建草稿→路由预览→提交→逐节点审批→表决）、`ccr-seed/verify.py`（结果验证）。

### 12.2 部门分流账号（§2.1 增补，§D16a 矩阵透出 dept_code）

GM 节点按分项 `dept_code` 匹配 `ccr_node_assignee` DEPT 指派（100101:dept_gm / 100102:dept_gm / 100103:dept_gm）；VP 节点按 `ccr_dept_vp` 部门映射（100101/100103→1022、100102→1023）；`dept_code` 为空时走 §2.1 原账号（deptgm/vicepresident）。

| 分项 dept_code | GM 节点 | VP 节点 | 场景示例 |
|---|---|---|---|
| 100101（对公存量） | deptgm | vicepresident_gsb | S3 |
| 100102（对公新增） | deptgm_sxsb | vicepresident_sxsb | S2 |
| NULL（上会兜底/存款） | deptgm | vicepresident | S1/S4/S5 |

### 12.3 造数场景与验证结果（2026-08-12，申请号/分项号可复测）

| 场景 | 申请号 | 分项号 | 客户/利率 | 冻结链 | 推进路径 | 终态 | 覆盖 |
|---|---|---|---|---|---|---|---|
| S1 BM-END | CCR202608124C12 | PI-D964EEEF | CUST003 NEW 3.5% | [BM] | 支行权限内终审 | 分项 APPROVED_LEVEL / 申请 FINAL | #4 权限内终审 |
| S2 VP-PENDING | CCR20260812F261 | PI-22D475BF | CUST003 NEW 3.3% | [BM,GM,VP] | BM 超权限→GM(100102)→停 VP | ROUTING @VP | 逐级上送、部门分流 |
| S3 MATRIX-SKIP-VP | CCR202608120107 | PI-218BA677 | CUST001 EXISTING GE5000 3.3%(原4.0) | [BM,VP] | BM 超权限→沿冻结链直达 VP（跳 GM），VP 权限内终审 | VP 终审(3.3%≥VP线3.3%) | #2/#4 冻结链推进 |
| S4 VOTING | CCR2026081219DE | PI-C0DB5CA9 | CUST003 NEW 3.1% | [BM,GM,VP,GROUP,PRESIDENT] | 逐级超权限→上会小组（3/6 委员已投） | VOTING | #6 表决保密 |
| S5 DEPOSIT | CCR20260812A2EB | PI-70873E59 | CUST001 存款1Y 1.3% | [BM,GROUP] | 支行过手→上会小组（0/6 票） | VOTING | 存款链、上会 |

### 12.4 用户反馈专项用例（回归 #2/#4/#5/#6/#1）

**#2/#4 冻结链推进（矩阵跳 GM）**——S3：
1. 提交流程中 `route-preview` 与提交返回的 `routeChain` 均为 `[BRANCH_MANAGER, VICE_PRESIDENT, …]`（跳过 GM，因该客户类型无 GM 权限行）；
2. 支行行长（wangwu）对 3.3%（<3.4% 支行权限下限）通过后，分项 `current_node_code` = `VICE_PRESIDENT`，**未落到 DEPT_GENERAL_MANAGER**（旧逻辑用固定贷款链会走到 GM）；
3. 校验 `ccr_pricing_item.route_chain` 提交冻结，审批推进沿此链（与提交预览一致）。

**#6 表决统计保密（对委员隐藏）**——S4：
1. `GET /ccr/vote-results/{itemId}`：president 返回计票；committee/auditor 均 403（"计票结果仅行长/审计可见"）；
2. `GET /ccr/approval/history/{applicationId}`：president 视角 `voteRounds` 有轮次、`voteResults` 空（未计票）；committee 视角 `voteRounds/voteResults/presidentDecisions` 全为空——委员看不到已投人数与进度；
3. 行长决策前委员无法得知表决是否结束（票数后台统计，仅行长可查汇总）。

**#1 审批轨迹终态展示**——S1：`historyDetail.approvalActions` 含 `APPROVE BRANCH_MANAGER → APPROVED_LEVEL`，申请 `status=FINAL`；前端历史审批详情显示"已经审批完成"（非"审批中"）。

**#5 节点指派为空**：BRANCH_MANAGER / VICE_PRESIDENT 无有效指派（wangwu/deptgm 的 PERSON 指派已删），按角色兜底解析，审批链路正常（S1~S5 已隐含验证）。

### 12.5 后续操作指引（复测/续测）

- **S2**：vicepresident_sxsb 登录，对 PI-22D475BF 在 VICE_PRESIDENT 节点通过 → 权限内（3.3%≥3.2%）终审。
- **S3**：vicepresident_gsb 登录，对 PI-218BA677 在 VICE_PRESIDENT 节点通过 → 权限内终审（3.3%≥VP 线 3.3%）。注：vote_condition 为 AND 语义（GE_5000 **且** SOE 同时命中才上会），CUST001 非国企大额不命中 → 走矩阵分层 VP 终审，不再上会；上会条件命中场景改为"大额国企存量"（GE_5000+SOE，见 05 记录 §12 试算）。
- **S4**：剩余 committee4/5/6 补投至 4 票 → 计票通过 → president 决策；验证行长可见汇总、委员不可见。
- **S5**：committee×6 投票 ≥4 票 → 计票 → president 决策。
- 全部终审后：验证决议（ccr_resolution）、合同/账户回填、承诺计划、通知、导出权限。
