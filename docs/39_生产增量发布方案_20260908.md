# 生产增量发布方案 v2.0.3（2026-09-08）—— docs/37(v2.0.2) 之后增量

> **适用范围**：生产已按 **docs/37 生产增量发布方案（2026-09-05）部署至 tag `v2.0.2` = f2be703** 的**保留库增量升级**。本次发 **f2be703 之后全部 27 笔实质提交**（09-05~09-08，含 09-07 经 merge 同步回 main 的决议预览/网关凭证改造 959f713、b95aefa），后端 jar + 前端 dist 整体替换。
> **本批定位**：非小步热修，而是 **v2.0.2 之后的功能批量收口**——决议书查询专岗、集团批量（存量拆分带出/调息上限/贡献度分指标）、决议书中文字体根治、SSO 统一认证、网关凭证环境变量化、节点指派白名单收口等，跨 09-05~09-08 多主题，一次发布随本方案锁定。
> 代码锁定：发布代码以 **tag `v2.0.3`**（打于含本方案 docs/39 的提交）锁定，随包推送 Gitea(88.93.0.37:3000) + GitHub。资产时间戳见 §二。
> 工作区干净（仅 `backend;C/`、`backend_20260902.zip`、`frontend/dist.tar` 3 个不入库产物）。

---

## 一、本次变更范围（相对基线 tag `v2.0.2` = f2be703）

| 分组 | 变更 | 需求/背景 | 提交 | 层级 | 需 SQL |
|---|---|---|---|---|---|
| **决议书查询专岗** | 新增专用角色 `resolution_query` 与「决议书查询」页：按 客户名称/客户号(兼集团号)/决议书编号 模糊查询「当前有效决议」并下载决议书 PDF；全量可见但仅列有效执行状态；无档案 JSON 权限 | #531 | f069e4c + 1e008b6 + ab6bff4 | jar+dist | **20260908_001** |
| **决议书 PDF 中文字体根治** | `ResolutionPdfExporter.loadFont` classpath→外部字体目录→系统字体三级查找容错；simhei/simfang 字体(~20MB)入库随 jar 内置，根治干净 git 构建缺字致决议书无法生成 | 生产事故 #521 | 3f9c836 + f49d494 | jar | 无 |
| **集团存量拆分带出(#522/523/524)** | 后端 `GET /ccr/groups/{groupNo}/splits` 整单带出存量拆分项；贷款申请前端按协议自动带出 | 集团批量 | 195f3d8 + 8f1aceb | jar+dist | 无 |
| **存量调息/分项上限校验(#527/528)** | 贷款提交校验：分项合计改「不得超过」上限、存量调息申请利率不得高于原利率；申请页同步展示上限 | 集团批量 | 5d7354e + 8f1aceb | jar+dist | 无 |
| **集团决议书按实际展示(#526)** | 集团决议书客户区走集团专属字段、空值不渲染，利率调整表分项带金额 | 集团批量 | bab4be3 | jar | 无 |
| **集团贡献度分指标(#534)** | 档案/审批详情「贡献度参考」：集团客户号按 group_no 读数仓 GROUP 批(排除 TOTAL)，按分指标展示 | 集团批量 | 478fe9e | jar+dist | 无 |
| **档案他行融资人工行(#535)** | 集团历史申请档案他行融资展示人工补录/Excel 行(后端按数仓同机构过滤，数仓为空时不丢弃人工行) | 生产问题 | 478fe9e + 361aa99 | jar+dist | 无 |
| **集团展示收敛** | 档案/审批详情申请内容卡客户号/集团号互斥展示；档案授信信息卡按集团综合授信；存款档案分项表对齐审批界面(续) | 展示收敛 | 91379e7 + 7dc9340 | dist | 无 |
| **行长整单决策主卡重构(#512/513/514)** | 行长决策整单主卡：分项完整列表/委员匿名意见平铺/结构化确认弹窗；工作台待办卡数值按去重申请 | 整单化 | 40f8feb | dist | 无 |
| **六人小组否决不签发决议书(#516)** | 仅批准类终态生成决议书，否决类只聚合主申请 REJECTED；被否决申请不产生决议/决议书 | 流程行为 | d0ddc25 | jar | 无 |
| **生产 Warm-Flow NPE 根治(#518)** | ensureFlow 已发布定义按标准链自愈补齐缺失 PASS 出边 | 生产问题 | b97c7f6 | jar | 无 |
| **流程优化** | 放宽授信分项校验 + 修复审批人预览 | 流程优化 | 422837d | jar+dist | 无 |
| **审批轨迹节点中文** | 工作台「我的申请动态」当前节点编码转中文(含综合支行长 PARENT_BRANCH_MANAGER) | 展示 | ab98ca7 | jar | 无 |
| **单户重提快照兜底** | 重提新草稿复制提交快照(customerInfoJson/creditInfoJson)，继续编辑不再被数仓重查清空年收入/职业等 | 数据兜底 | 17a52f7 | jar+dist | 无 |
| **SSO 统一认证登录** | admin 除外注入凭证(env)后走统一认证验密，配置驱动缺凭证自动回退本地 BCrypt | #529 | 9fe426f | jar | 无(见 §三 前置) |
| **网关凭证 env 化** | 授信决议网关凭证 secret→appId+apiKey 双头认证 + token 路径改 `/miniapp/auth/token`；凭证改由 `CCR_INTEGRATION_CREDIT_RESOLUTION_*` 环境变量注入，默认关闭 | 配置安全 | 959f713 + 458a9e1 | jar | 无(见 §三 前置) |
| **决议预览 + 打包入口** | 第四环节决议预览 + Windows `./dev release` 生产更新包打包入口 | 交付工具 | b95aefa | jar+dist | 无 |
| **节点指派白名单收口** | 节点指派候选人仅支行行长/部门总经理/分管行长三类角色(前端下拉 + 后端保存/委托双收口) | #532 | 3805fea | jar+dist | 无 |

> **结论**：本次 **1 份数据/配置 SQL**（`db/incr/20260908_001_resolution_query.sql`，#531 菜单/角色/测试账号/岗位种子，幂等）。**无表结构 DDL**。`db/20_clean_biz.sql`(#519 保留 flow_skip)为测试环境手工清理脚本，**不入生产**。**无 Redis key 需清理**。
> 配套 docs/38（f04e0a0）为「集团存量申请自动带出成员拆分项」方案文档，本批 #522-528 已按其落地，文档随代码同步入库。

---

## 二、发布资产清单

| 资产 | 路径 | 构建/修改时间 | 说明 |
|---|---|---|---|
| 后端 jar | `backend/ccr-admin/target/ccr-admin.jar` | 2026-09-08 17:54 | 覆盖至 1e008b6 全部后端源码；**已内置 simhei/simfang 字体**(`BOOT-INF/lib/ccr-approval-1.0.0.jar!/fonts/`)，生产无需再跑 `patch-fonts.py` |
| 前端 dist | `frontend/dist/` | 2026-09-08 15:31 | 覆盖至 ab6bff4 全部前端源码（决议书查询页去「执行状态」列、白名单收口、集团贡献度分指标、他行融资人工行等） |
| 数据变更 SQL | `release/ccr_incr_20260908.sql` | 2026-09-08 | 单份 20260908_001 幂等种子，一键执行（不入 git，随包本地传递） |
| 代码标签 | tag `v2.0.3` | 2026-09-08 | 锁定含本方案 docs/39 的发布代码；推送 Gitea + GitHub |

> ⚠️ **测试账号 resq(郑决议查询)**：20260908_001 内建测试账号仅限验证 resolution_query 角色链路，密码=统一初始密码 `Yxnsh@1a3s`。**生产正式使用须通过正式用户管理流程创建并改密**；SQL 幂等可重复执行。

---

## 三、部署前检查 Checklist

- [ ] **备份生产库**（SQL 步骤前必做）：
  ```bash
  docker exec ccr-prod-mysql sh -c 'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" --single-transaction --routines --triggers ccr_rate' | gzip > prod_backup_$(date +%Y%m%d_%H%M).sql.gz
  ```
- [ ] **备份当前容器内运行产物**（回滚备用）：
  ```bash
  mkdir -p ./backup
  docker cp ccr-prod-backend:/app/app.jar ./backup/app.jar.bak
  docker cp ccr-prod-frontend:/usr/share/nginx/html ./backup/html.bak
  ```
- [ ] 确认三容器运行中：`docker ps | grep ccr-prod`
- [ ] 确认生产当前版本为 v2.0.2（`docker exec ccr-prod-backend sh -c 'ls -l /app/app.jar'` 时间戳早于本次 17:54 jar）
- [ ] **SSO/网关凭证部署前置（可选，按生产实际）**：
  - **SSO 统一认证**：仅当生产要启用统一认证登录时，在部署环境注入 `CCR_INTEGRATION_AUTH_API_KEY`（url 默认 `http://32.13.6.6:8000/authing/login`，如生产网关地址不同需同时注入 `CCR_INTEGRATION_AUTH_URL`）。**不注入则自动回退本地 BCrypt，登录行为不变、不锁死**，可完全跳过。
  - **授信决议网关**：仅当生产要启用与 Mini-App-Plus 的授信决议网关集成时，注入 `CCR_INTEGRATION_CREDIT_RESOLUTION_ENABLED=true` + `_GATEWAY_BASE_URL` + `_APP_ID` + `_API_KEY` 等（双头认证，默认关闭）。若生产此前通过外部 `config/application-prod.yml` 覆盖网关配置，升级后仍以外部挂载配置为准，无需动作。
  - 以上 env 均**严禁写死进代码/镜像/仓库**，仅部署侧注入。

---

## 四、数据变更（单份幂等种子，推荐直接用汇总文件）

```bash
# 预检:当前 id 分配未冲突(应输出 0 行)
docker exec -i ccr-prod-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD" --default-character-set=utf8mb4 ccr_rate' -e "
SELECT 'menu14' k, COUNT(*) FROM ccr_sys_menu WHERE id=14
UNION ALL SELECT 'role2012', COUNT(*) FROM ccr_sys_role WHERE id=2012
UNION ALL SELECT 'resq', COUNT(*) FROM ccr_sys_user WHERE username='resq';"
```
（菜单 14/角色 2012/账号 resq 均应为 0，防覆盖已有业务 id；若非 0 先核对归属）

```bash
# 方式一(推荐):汇总文件一次跑完
docker cp release/ccr_incr_20260908.sql ccr-prod-mysql:/tmp/ccr_incr_20260908.sql
docker exec -i ccr-prod-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD" --default-character-set=utf8mb4 ccr_rate' < /tmp/ccr_incr_20260908.sql

# 方式二:直接执行 db/incr 单份
docker exec -i ccr-prod-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD" --default-character-set=utf8mb4 ccr_rate' < db/incr/20260908_001_resolution_query.sql
```

**执行后复验**：菜单 14 / 角色 2012(resolution_query, menu_ids 含 1,14)/ 账号 resq 与岗位绑定 17 均存在；admin 角色 menu_ids 已含 14（幂等 UPDATE）。

> 本次无表结构变更，SQL 后无需清 Redis；对已登录会话即时生效（新角色/菜单读库）。

---

## 五、后端部署

**路径 A（docker cp 直接替换，推荐，无需重建镜像）**：
```bash
docker cp backend/ccr-admin/target/ccr-admin.jar ccr-prod-backend:/app/app.jar
docker restart ccr-prod-backend
# 等待启动完成(约 20-40s),健康检查:
curl -s http://localhost:8080/health    # 期望 {"code":200,...,"status":"UP"}
```

**重启后流程三表自动填充**（后端首次启动 `StandardFlowInitializer` 重建流程定义，无需手工 SQL）：
```bash
docker exec -i ccr-prod-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD"' -N -e "
SELECT CONCAT('flow_definition=', COUNT(*)) FROM ccr_rate.flow_definition WHERE is_publish=1;
SELECT CONCAT('flow_node=', COUNT(*)) FROM ccr_rate.flow_node;
SELECT CONCAT('flow_skip=', COUNT(*)) FROM ccr_rate.flow_skip;
"
# 期望:flow_definition=2、flow_node>=13、flow_skip>=6
```

> **字体**：本 jar 已内置 simhei/simfang（BOOT-INF 依赖 jar `fonts/` 目录，loadFont 走 classpath 命中），**生产无需再执行 `release/patch-fonts.py` 或手工 docker cp 字体**；若历史容器内残留旧 `/app/fonts` 不影响。`release/patch-fonts.py` 保留为将来字体再次缺失时的兜底工具。

---

## 六、前端部署

**路径 A（docker cp 直接替换）**：
```bash
# 先清空旧静态文件,避免残留旧版本 index.html 引用
docker exec ccr-prod-frontend sh -c 'rm -rf /usr/share/nginx/html/*'
docker cp frontend/dist/. ccr-prod-frontend:/usr/share/nginx/html/
# 验证首页可访问:
curl -s -o /dev/null -w "%{http_code}\n" http://localhost/    # 200
```

**浏览器侧**：发布后强制刷新（Ctrl+F5）。

---

## 七、验证要点（按变更逐项，生产验收口径）

| 变更 | 验证操作 | 期望结果 |
|---|---|---|
| 冒烟 | 各角色登录 → 首页 + 登录接口 | HTTP 200，无报错 |
| 决议书查询(#531) | 用 resq(郑决议查询)登录 → 左侧出现「决议书查询」；按客户名称/客户号(兼集团号)/决议书编号 单查与组合、翻页 | 仅列有效执行状态决议(已签发/执行中等)；被否决/关闭决议不出现；集团号能查到集团决议；下载 PDF 可打开且中文正常（文件名 客户名_签发日_申请号.pdf） |
| 决议书查询权限边界 | resq 访问某申请档案 JSON 详情接口 | 403（无档案权限）；customer_manager 登录看不到菜单、curl query 被 403 |
| 决议书中文字体(#521) | 生成/下载任一决议书 PDF | 标题/正文中文正常，无「□」缺字 |
| 集团贡献度分指标(#534) | 打开集团申请档案/审批详情 → 贡献度参考 | 按 deposit/loan/payroll/ratio 等分指标展示（不显示综合总额 TOTAL 行），数值与数仓 GROUP 批一致 |
| 档案他行融资(#535) | 打开集团历史申请档案 → 他行融资 | 数仓明细 + 人工补录/Excel 行均展示，来源列正确；数仓为空时人工行不丢 |
| 存量拆分带出/调息上限(#522-528) | 发起集团贷款申请(存量协议) → 提交校验 | 分项按协议自动带出；分项合计超上限、存量调息利率高于原利率均被拦截 |
| 集团决议书展示(#526) | 打开集团决议书预览/下载 | 客户区走集团字段、空值不渲染，利率调整表分项带金额 |
| 行长整单决策(#512-514) | 行长角色处理整单待办 → 决策弹窗 | 分项完整列表/委员匿名意见平铺；工作台待办卡数值按去重申请 |
| 否决不签发(#516) | 走一笔被否决申请全链路 | 最终无决议书生成，决议查询/决议列表无该单 |
| 流程自愈(#518) | 触发已发布流程实例执行 | 无 Warm-Flow NPE，缺失 PASS 出边自愈补齐 |
| 审批轨迹中文 | 工作台「我的申请动态」查看当前节点 | 显示中文节点名(如 综合支行长/部门总经理)，非编码 |
| 单户重提兜底 | 存量单户草稿继续编辑/重提 | 年收入/职业/证件号等不被数仓重查清空 |
| 白名单收口(#532) | 流程配置→节点指派选人下拉 | 仅 支行行长/部门总经理/分管行长 三类；curl 以 customer_manager 保存 PERSON/ROLE/GROUP → 400，白名单角色 → 成功 |
| 节点指派回归 | 既有 PERSON/DEPT 指派行编辑/解析 | 回显可保存，解析预览结果与发布前一致（白名单未触碰 resolver） |
| SSO(如启用) | admin 除外用统一认证账号登录 | 走统一认证验密；未注入凭证环境 → 本地 BCrypt 登录正常 |
| 回归 | 存量综合支行贷款/集团流程/存款/六人小组/秘书岗/行长决策 | 与发布前一致，本轮未触碰流程路由主体与利率矩阵 |

> 若先在测试环境做发布前回归（推荐），测试库 mock 数仓 `data_dt` 须在提交前 3 个自然日内（否则 DATA_TIMELINESS 拦截）。

---

## 八、回滚预案

| 步骤 | 操作 |
|---|---|
| 后端回退 | `docker cp app.jar.bak ccr-prod-backend:/app/app.jar` + `docker restart ccr-prod-backend` |
| 前端回退 | `docker exec ccr-prod-frontend sh -c 'rm -rf /usr/share/nginx/html/*'` → `docker cp html.bak/. ccr-prod-frontend:/usr/share/nginx/html/` |
| SQL 回退 | 20260908_001 为**新增**种子（菜单14/角色2012/账号resq/岗位17），未改动既有行：回退时删除即可 `DELETE FROM ccr_sys_user_post WHERE id=17; DELETE FROM ccr_sys_user WHERE username='resq'; DELETE FROM ccr_sys_role WHERE id=2012; DELETE FROM ccr_sys_menu WHERE id=14;` 并还原 admin menu_ids(去掉尾部 ,14) |
| 缓存 | 回退后同样需浏览器强刷 |

---

## 九、安全边界

- **严禁 push GitHub/Gitea**：`db/prod_full*.sql`（含真实账号/机构/密码哈希）、生产 `config/application-prod.yml`、`docker-compose.prod.yml` 属敏感文件，均已入 `.gitignore`。
- 本次 SSO/网关凭证一律走环境变量注入（`CCR_INTEGRATION_AUTH_*` / `CCR_INTEGRATION_CREDIT_RESOLUTION_*`），**代码与默认配置不落任何真实凭证**；默认关闭，缺凭证自动回退本地/不启用，不锁死现有登录。
- 决议书查询专岗 `resolution_query` 仅授予查询页与 PDF 下载权限，**无档案 JSON 查看权**（`ApplicationAccessService.authorizeView` 未放行该角色）。
- 20260908_001 中测试账号 resq 为验证链路所建，生产正式用户须另行建档；`release/` 汇总不入 git，随包本地传递。
- 生产数据库密码/config 仅存部署侧，不入库。
