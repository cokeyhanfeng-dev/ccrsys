# 生产增量发布方案 v2.0.2（2026-09-05）—— docs/36 之后增量

> **适用范围**：生产已按 **docs/36 生产增量发布方案（2026-09-05）部署完毕**（tag `v2.0.1` = 690728c，表结构增量已执行）的**保留库增量升级**。本次只发 **docs/36 之后改动**（6 笔提交），后端 jar + 前端 dist 整体替换，**数据库无表结构 SQL**（不含任何 DDL/幂等建表），唯一数据变更为 **王翼 GM 登录工号 02300328→02300325 的一条 UPDATE**（§四，按生产实际执行）。
> 定位说明：本轮为**小步增量收口**——生产实报三个展示/计数问题（今日已办/委员卡片名/机构树徽标）+ 存量个人客户五级分类带不出 + 档案存款展示差异，全部为代码修复，随 jar/dist 发布即生效。
> 代码锁定：发布代码以 **tag `v2.0.2`**（打于含本方案 docs/37 的提交）锁定，随包推送 Gitea/GitHub；资产时间戳见 §二。
> 工作区干净（仅 2 个本地构建产物 zip/tar 不入库）。

---

## 一、本次变更范围（相对基线 tag `v2.0.1` = 690728c）

| 变更 | 需求/背景 | 层级 | 需 SQL |
|---|---|---|---|
| **工作台「今日已办」恒为 0**：与累计/历史参与口径对齐（今日审批 action ∪ 本人表决 ballot ∪ 本人行长决策，按申请去重；旧实现仅计 `ccr_approval_action`，委员表决/行长决策永不计入） | 生产实报 d5c405f | jar+dist | 无 |
| **六人小组待办卡片显示客户编号而非客户名称**：整单化后卡片取客户号，改取客户名称 | 生产实报 031945f | jar+dist | 无 |
| **机构树零售支行徽标被右缘遮挡**：树节点 flex 布局修复 | 生产实报 18d22ed | dist | 无 |
| **存量个人客户五级分类带不出（贷款利率申请）**：`CustomerController.detail()` 个人分支 SELECT 漏查 `ffthlv_class` 列 → `fiveLevelClass` 缺失（对公分支与 auto-backfill 路径本有）；补 `ffthlv_class fiveLevelClass`。电话 `tel_no` 一直在查、dev 实测可带出；若生产电话仍空请先自查数仓该客户最新批次 `tel_no` 是否有值 | 生产实报 1a30793 | jar | 无 |
| **历史档案存款申请展示与审批详情不一致**：档案「授信信息/申请材料附件/关联人/决议执行核验/历史履约」卡及授信分项「担保方式」列未按 `isLoan` 裁剪（存款无授信/附件/关联人/担保概念），对存款渲染贷款语义空卡；按审批详情口径对齐 | 生产实报 4f852ac | dist | 无 |
| **公司金融部总经理王翼登录工号 02300328→02300325**：`ccr_sys_user` 仅 `username`（登录名）变更，id 2088225316330213388 不变；`ccr_sys_user_post` 按 user_id、`ccr_node_assignee` 部门 GM 为 DEPT `3202233912:dept_gm`（部门:岗位）均不引用工号，无需动 | 配置调整 82a8543 | **DB UPDATE**（§四） | 一条 UPDATE |

> **结论**：本次**无表结构增量 SQL**（§四仅 1 条数据 UPDATE，可选项），无需清 Redis；不含任何测试数据/种子。

---

## 二、发布资产清单

| 资产 | 路径 | 构建/修改时间 | 说明 |
|---|---|---|---|
| 后端 jar | `backend/ccr-admin/target/ccr-admin.jar` | 2026-09-05 16:08 | `main`（含 #510 个人五级分类修复）重编产物，晚于全部后端源码；含今日已办/委员卡片名等全部后端改动 |
| 前端 dist | `frontend/dist/` | 2026-09-05 16:07 | 本轮 `npm run build`，晚于最后前端源码（含 #509 档案对齐/机构树徽标/委员卡片名/今日已办前端） |
| 数据变更 SQL | §四 单条 UPDATE | 2026-09-05 | 王翼工号 02300328→02300325（幂等定位 id+nick_name；不入 `release/` 汇总，直接按 §四 执行） |
| 代码标签 | tag `v2.0.2` | 2026-09-05 | 锁定含本方案 docs/37 的发布代码；推送 Gitea + GitHub |

> ⚠️ 本增量**无测试演示数据、无表结构 SQL**。

---

## 三、部署前检查 Checklist

- [ ] **备份生产库**（SQL 步骤前必做）：
  ```bash
  docker exec ccr-prod-mysql sh -c 'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" --single-transaction --routines --triggers ccr_rate' | gzip > prod_backup_$(date +%Y%m%d_%H%M).sql.gz
  ```
- [ ] **备份当前容器内运行产物**（回滚备用）：
  ```bash
  mkdir -p ./backup
  docker cp ccr-prod-backend:/app/app.jar ./backup/app.jar.bak
  docker cp ccr-prod-frontend:/usr/share/nginx/html ./backup/html.bak
  ```
- [ ] 确认三容器运行中：`docker ps | grep ccr-prod`
- [ ] 确认生产当前版本为 v2.0.1（`docker exec ccr-prod-backend sh -c 'ls -l /app/app.jar'` 时间戳早于本次 16:08 jar）

---

## 四、数据变更（王翼工号，按生产实际执行）

> 非表结构增量。**若生产 ccr_sys_user 该账号已改**（例如已由人事同步/手工改过）则跳过；否则执行后王翼须改用 `02300325` 登录，`02300328` 立即失效，密码哈希不变（已改密继续有效，初始密码 `Yxnsh@1a3s` 同理）。
> 工号在系统内只作登录名 + 「按人指派（PERSON/代理）」引用；王翼不是六人委员/行长/秘书，`ccr_node_assignee` 中部门总经理为部门级 DEPT `3202233912:dept_gm`（org 1040:岗位），不引用其工号，解析不受影响。

```sql
-- ① 预检:新工号必须未被他人占用(唯一键)
SELECT id, username, nick_name FROM ccr_sys_user WHERE username IN ('02300328','02300325');
-- ② 核对 node_assignee 是否曾按 PERSON 引用王翼旧工号(预期无行)
SELECT id, node_code, assignee_type, assignee_code, delegate_to, status
FROM ccr_node_assignee WHERE assignee_code='02300328' OR delegate_to='02300328' AND del_flag='0';
-- ③ 执行(id 定位防误伤)
UPDATE ccr_sys_user SET username='02300325'
 WHERE username='02300328' AND id=2088225316330213388 AND nick_name='王翼';
-- ④ 复验
SELECT id, username, nick_name, role_code, org_id, status FROM ccr_sys_user WHERE id=2088225316330213388;
```

---

## 五、后端部署

**路径 A（docker cp 直接替换，推荐，无需重建镜像）**：
```bash
docker cp backend/ccr-admin/target/ccr-admin.jar ccr-prod-backend:/app/app.jar
docker restart ccr-prod-backend
# 等待启动完成(约 20-40s),健康检查:
curl -s http://localhost:8080/health    # 期望 {"code":200,...,"status":"UP"}
```

**重启后流程三表自动填充**（后端首次启动 `StandardFlowInitializer` 重建流程定义，无需手工 SQL）：
```bash
docker exec -i ccr-prod-mysql sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD"' -N -e "
SELECT CONCAT('flow_definition=', COUNT(*)) FROM ccr_rate.flow_definition WHERE is_publish=1;
SELECT CONCAT('flow_node=', COUNT(*)) FROM ccr_rate.flow_node;
SELECT CONCAT('flow_skip=', COUNT(*)) FROM ccr_rate.flow_skip;
"
# 期望:flow_definition=2、flow_node>=13、flow_skip>=6
```

---

## 六、前端部署

**路径 A（docker cp 直接替换）**：
```bash
# 先清空旧静态文件,避免残留旧版本 index.html 引用
docker exec ccr-prod-frontend sh -c 'rm -rf /usr/share/nginx/html/*'
docker cp frontend/dist/. ccr-prod-frontend:/usr/share/nginx/html/
# 验证首页可访问:
curl -s -o /dev/null -w "%{http_code}\n" http://localhost/    # 200
```

**浏览器侧**：发布后强制刷新（Ctrl+F5）。

---

## 七、验证要点（按变更逐项，生产验收口径）

| 变更 | 验证操作 | 期望结果 |
|---|---|---|
| 冒烟 | 各角色登录 → 首页 + 登录接口 | HTTP 200，无报错 |
| 今日已办（d5c405f） | 工作台首页「今日已办」计数 | 本人今日审批/表决/决策过 1 笔则显示 ≥1，与「累计已办」分页口径一致，不再恒 0 |
| 委员卡片名（031945f） | 六人小组委员登录 → 待办卡片 | 卡片显示客户名称而非客户编号 |
| 机构树徽标（18d22ed） | 系统管理→机构树，展开含零售支行 | 支行性质徽标完整显示，不再被行尾遮挡 |
| 个人五级分类（1a30793） | 发起贷款利率申请 → 选存量**个人**客户 | 「五级分类」下拉带出（正常/关注/…），联系电话带出；若电话仍空，按 §一 自查数仓 `caps_indv_cust_basic_info` 最新批次该客户 `tel_no` |
| 档案存款对齐（4f852ac） | 历史审批 → 打开一笔**存款**申请档案 | 与审批详情一致：不显示「授信信息/申请材料附件/关联人/担保方式列」等贷款语义区块；贷款档案展示不变 |
| 王翼工号（§四） | 公司金融部总经理用 `02300325` 登录 → 对公存量单 | 登录成功，GM 待办正常（部门级 DEPT 指派不受影响）；旧工号 `02300328` 登录失效 |
| 回归 | 存量综合支行贷款/集团流程/存款/行长决策/秘书岗 | 与发布前一致，本轮未触碰流程/路由/矩阵 |

> 若先在测试环境做发布前回归（推荐），测试库 mock 数仓 `data_dt` 须在提交前 3 个自然日内（否则 DATA_TIMELINESS 拦截），仅涉及 mock 数据日期，与本次 SQL 无关。

---

## 八、回滚预案

| 步骤 | 操作 |
|---|---|
| 后端回退 | `docker cp app.jar.bak ccr-prod-backend:/app/app.jar` + `docker restart ccr-prod-backend` |
| 前端回退 | `docker exec ccr-prod-frontend sh -c 'rm -rf /usr/share/nginx/html/*'` → `docker cp html.bak/. ccr-prod-frontend:/usr/share/nginx/html/` |
| 工号 UPDATE | 如执行后需还原：`UPDATE ccr_sys_user SET username='02300328' WHERE username='02300325' AND id=2088225316330213388 AND nick_name='王翼';` |
| 缓存 | 回退后同样需浏览器强刷 |

---

## 九、安全边界

- **严禁 push GitHub/Gitea**：`db/prod_full*.sql`（含真实账号/机构/密码哈希）、生产 `config/application-prod.yml`、`docker-compose.prod.yml` 属敏感文件，均已入 `.gitignore`。
- 本次无表结构 SQL、无测试数据；§四 单条 UPDATE 为真实账号数据变更，仅在生产按需手工执行，不入 `release/`、不入 git。
- 生产数据库密码/config 仅存部署侧，不入库。
