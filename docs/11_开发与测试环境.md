# 开发与测试环境

## 1. 隔离原则

- 宿主机只复用满足版本要求的 Node、Maven、Docker Desktop。
- JDK 17 安装在项目 `.tools/`，不会修改系统 `JAVA_HOME`、`PATH` 或默认 Java。
- Maven 依赖写入项目 `.cache/m2/repository`，不会写入 `~/.m2/repository`。
- npm 缓存写入项目 `.cache/npm`，依赖写入 `frontend/node_modules`。
- MySQL、Redis 以及完整测试应用只运行在 Docker 的 `ccrsys-test` 项目中。
- 测试容器不会随 Docker Desktop 自动重启，统一通过 `./dev infra-up` 或 `./dev app-up` 显式启动。
- `.tools/`、`.cache/`、`node_modules/`、`dist/`、`target/` 均不提交 Git。

当前端口均仅绑定 `127.0.0.1`：

| 服务 | 地址 |
|---|---|
| 测试前端 | http://127.0.0.1:13000 |
| 测试后端 | http://127.0.0.1:18080 |
| 测试 MySQL | 127.0.0.1:23306 |
| 测试 Redis | 127.0.0.1:26379 |

### 测试账号（初始密码统一 `Yxnsh@1a3s`，首次登录后强制修改）

| 用户名 | 角色 | 机构(org_id) | 备注 |
|---|---|---|---|
| `zhangsan` | 客户经理 customer_manager | 1001 | **CUST001 管户经理**（测管户权限/存量拆分项用它） |
| `lisi` | 客户经理 customer_manager | 1007 | **CUST003 管户经理**（测"非管户拉不出"用它） |
| `wangwu` | 支行行长 branch_manager | 1001 | 审批首节点 |
| `deptgm` | 部门总经理 dept_gm | 1003 | 审批链 GM 节点 |
| `committee` | 小组成员 committee_member | 1000 | 六人小组 |
| `committee2`~`committee6` | 小组成员 committee_member | 1003 | 六人小组其余成员 |
| `president` | 总行行长 president | 1000 | 行长决策终审 |
| `vicepresident` | 分管行长 vice_president | 1000 | |
| `reviewer` | 配置复核人 config_reviewer | 1000 | |
| `auditor` | 审计人员 auditor | 1000 | |
| `contractor` | 合同经办 contract_operator | 1000 | |
| `admin` | 系统管理员 admin | 1000 | 后台管理 |

## 2. 首次初始化

Windows 轻量打包使用根目录 `release.cmd [1|2|12]`，工具预检使用 `release.cmd 12 -CheckOnly`。需要 Windows PowerShell 5.1+、Git、tar，以及所选构建对应的 JDK 17/Maven 3.9 或 Node 20+；后端默认执行测试。项目内工具路径、会话级 `CCR_JAVA_HOME` 和缓存隔离详见 `docs/31_离线增量部署手册.md` 的 Windows 小节。此入口仅构建 Jar/dist，不启动或重建测试容器。

先启动 Docker Desktop，然后执行：

```bash
./dev setup
./dev versions
```

当前 macOS arm64 环境会下载经过 SHA-256 校验的 Temurin JDK 17.0.20+8。系统 Maven 3.9.x 和 Node 20+ 可直接复用；缺少兼容 Maven 时，引导脚本会下载经过 SHA-512 校验的 Maven 3.9.16 到 `.tools/maven`。

## 3. 日常开发

只启动基础设施：

```bash
./dev infra-up
```

本地启动后端和前端：

```bash
./dev backend-run
./dev frontend-run
```

后端使用 `local` profile，连接 Docker 中的 MySQL/Redis。前端开发服务器仍位于 http://127.0.0.1:3000，代理到本地后端 `8080`。

## 4. 完整测试环境

```bash
./dev app-up
./dev smoke
./dev ps
```

登录冒烟默认使用隔离测试环境的统一初始口令并校验首登链路；需要覆盖时通过当前命令的
`CCR_SMOKE_PASSWORD` 环境变量传入，禁止将生产口令写入脚本或提交到仓库。

`app-up` 会构建并启动 MySQL、Redis、Spring Boot、Nginx/Vue 四个容器。首次启动 MySQL 时由
`scripts/dev/mysql-init.sh` 按以下顺序初始化：

1. 根目录中的基础结构、基础配置与基础种子（由脚本显式列出，包含 `01-09` 以及 `11`、`12`、`13`、`14`、`16`、`18`、`19`、`25`）；
2. `db/incr/*.sql`：按文件名升序执行全部增量；
3. 模拟数据、测试数据及清理脚本（`10`、`15`、`17`、`20`、`22`、`26`）。

`db/incr_prod_*.sql` 是存量生产环境的聚合升级脚本，不参与全新测试库初始化。

停止容器并保留测试数据：

```bash
./dev app-down
```

清空专用测试数据库并重新初始化：

```bash
./dev reset
./dev app-up
./dev smoke
```

`reset` 只删除 Compose 项目 `ccrsys-test` 的容器、网络和专用数据卷。

## 5. 提交前验证

```bash
./dev verify
```

它会使用项目 JDK/Maven 缓存执行后端全部单元测试，并使用兼容的本机 Node 执行 `npm ci` 与生产构建。涉及数据库、缓存、接口、权限、工作流或部署的改动还要执行：

```bash
./dev app-up
./dev smoke
```

查看日志：

```bash
./dev logs backend
./dev logs mysql
```
# Mini-App-Plus 授信决议 API 网关（2026-09-02）

该能力默认关闭，未配置时不影响原有申请。启用后需要在部署环境提供以下变量，接口契约和私有桶兑换要求见 `docs/30_API网关授信决议集成.md`。

```bash
CCR_INTEGRATION_CREDIT_RESOLUTION_ENABLED=true
CCR_INTEGRATION_CREDIT_RESOLUTION_GATEWAY_BASE_URL=https://api-gateway.internal.example
CCR_INTEGRATION_CREDIT_RESOLUTION_TOKEN_PATH=/miniapp/auth/token
CCR_INTEGRATION_CREDIT_RESOLUTION_APP_ID='<网关分配的应用标识>'
CCR_INTEGRATION_CREDIT_RESOLUTION_API_KEY='<至少16位，生产建议32字节以上随机API Key>'
CCR_INTEGRATION_CREDIT_RESOLUTION_ALLOWED_DOWNLOAD_HOSTS=minio.internal.example
```

统一认证与决议书网关是两套独立配置。需要同时启用时，环境文件可完整配置为：

```dotenv
# 统一认证登录
CCR_INTEGRATION_AUTH_ENABLED=true
CCR_INTEGRATION_AUTH_URL=https://auth.internal.example/authing/login
CCR_INTEGRATION_AUTH_APP_ID=example-auth-app-id
CCR_INTEGRATION_AUTH_API_KEY=replace-with-auth-secret
CCR_INTEGRATION_AUTH_APP_CODE=example-auth-app-code
CCR_INTEGRATION_AUTH_APP_ID_HEADER=X-App-Id
CCR_INTEGRATION_AUTH_API_KEY_HEADER=apikey

# 授信决议书查询、文件兑换与下载
CCR_INTEGRATION_CREDIT_RESOLUTION_ENABLED=true
CCR_INTEGRATION_CREDIT_RESOLUTION_GATEWAY_BASE_URL=https://api-gateway.internal.example
CCR_INTEGRATION_CREDIT_RESOLUTION_TOKEN_PATH=/miniapp/auth/token
CCR_INTEGRATION_CREDIT_RESOLUTION_LATEST_PATH=/miniapp/creditResolution/ccr/latest
CCR_INTEGRATION_CREDIT_RESOLUTION_EXCHANGE_PATH=/miniapp/creditResolution/ccr/files/exchange
CCR_INTEGRATION_CREDIT_RESOLUTION_APP_ID=example-resolution-app-id
CCR_INTEGRATION_CREDIT_RESOLUTION_API_KEY=replace-with-resolution-secret
CCR_INTEGRATION_CREDIT_RESOLUTION_APP_ID_HEADER=X-App-Id
CCR_INTEGRATION_CREDIT_RESOLUTION_API_KEY_HEADER=apikey
CCR_INTEGRATION_CREDIT_RESOLUTION_ALLOWED_DOWNLOAD_HOSTS=minio.internal.example
```

Docker Compose 启动时可将这些变量写入服务器上的独立文件，例如
`/data/ccr/config/integration.env`（权限建议设为 `600`，不要放入代码仓库）：

```bash
chmod 600 /data/ccr/config/integration.env
```

启动后端服务：

```bash
docker compose --env-file /data/ccr/config/integration.env up -d backend
```

需要同时构建并启动全部服务时：

```bash
docker compose --env-file /data/ccr/config/integration.env up -d --build
```

查看 Compose 最终生成的启动配置：

```bash
docker compose --env-file /data/ccr/config/integration.env config
docker compose ps
```

`docker compose config` 会展开环境变量，其中可能包含 API Key，仅在受控终端查看，不要复制到日志或工单。
已运行容器可通过 `docker inspect ccr-backend` 查看 Docker 保存的容器配置；其中同样包含环境变量明文。

本项目隔离测试栈由 `./dev app-up` 启动，底层命令可在根目录 `dev` 的 `app-up)` 分支查看。
测试栈需要注入网关参数时，直接在当前 shell 导出上述变量后运行 `./dev app-up`；
`compose.test.yml` 会把这些变量传入后端容器。

CCRSYS 使用 `X-App-Id/apikey` 并携带当前登录用户名（绩效码）换取该人员的 Mini-App-Plus Token，随后携带 Bearer Token 调用决议查询和文件兑换路由。令牌按绩效码分别缓存，到期前刷新，401 时只重新登录并重试一次。Mini-App-Plus 需启用 `MINIAPP_CCR_GATEWAY_AUTH_ENABLED`，配置与网关一致的 App ID/API Key，并给允许访问的人员绑定 `ccr_service` 角色。API 网关需要透传鉴权头并启用时间窗口、流水号防重放、限流和来源限制。禁止在仓库、Compose 文件和命令历史中填写真实 API Key、令牌、MinIO AccessKey 或 SecretKey；不得使用 `123456`、`root123` 等测试弱口令。

## 生产轻量打包与部署

开发机执行以下任一命令：

```bash
./dev release 1   # 仅后端 Jar
./dev release 2   # 仅前端 dist
./dev release 12  # 后端和前端
./dev release     # 交互选择 1/2/12
```

脚本在 `release/` 生成 `ccr-release-<时间>-<类型>.tar.gz` 和 `deploy-release.sh`。将两者上传至生产服务器 `/tmp`，执行：

```bash
bash /tmp/deploy-release.sh 1    # 仅后端
bash /tmp/deploy-release.sh 2    # 仅前端
bash /tmp/deploy-release.sh 12   # 全部
```

部署脚本自动选择 `/tmp` 中最新发布包、校验 SHA-256、备份旧 Jar/静态文件、替换并清理临时解压目录。默认后端容器自动识别 `ccr-prod-backend` 或 `ccr-backend`，也会尝试查找唯一包含 `/app/app.jar` 的运行容器；前端目录默认为 `/data/ccr/frontend`。环境不同可设置 `CCR_BACKEND_CONTAINER`、`CCR_BACKEND_JAR_PATH`、`CCR_FRONTEND_DIR`、`CCR_BACKUP_DIR`、`CCR_BACKEND_HEALTH_URL`。发布包留在 `/tmp` 便于核对和回滚，确认上线无误后可手工删除。

## 2026-09-09 移动审批入口

- `./dev mobile-run`：Vite 仅绑定 `127.0.0.1:13001`，访问 `/mobile/`；`CCR_MOBILE_DEV_UPSTREAM` 默认为 `http://127.0.0.1:18080`。
- `./dev mobile-build`：执行移动端回归并生成独立 `frontend/dist-mobile`；产物禁止提交。电脑端发布 ZIP 仍独立。
- `./dev frontend-test` 与 `./dev verify` 已包含移动单测和生产构建。`./dev app-up` 的原 frontend 服务同时托管 `/mobile/`，无新增容器和数据库；`./dev smoke` 增加移动入口/匿名401/越界代理404。
- 生产 Dockerfile 需要 `dist` 和 `dist-mobile` 两个构建目录，请先运行 `./dev frontend-test`；测试 Dockerfile 内部自动构建。
- 两套 Compose 显式透传 `CCR_MOBILE_YOUDU_ENABLED`（默认 false）、`CCR_MOBILE_YOUDU_VERIFY_URL`（包含一个 `{token}` 的固定 URL 模板）、`CCR_MOBILE_YOUDU_SUCCESS_CODE`（默认 0，部署前核实）。未启用不回退密码。连接配置只存部署侧环境文件，不提交真实地址或凭证。
- 移动 Nginx 独立部署可用 `frontend/mobile/nginx.conf`，只开放 `/mobile/api/mobile/**` 到 CCR；原后台服务和数据保留在生产区。
- 本次不涉及 DDL，不运行 reset，不重建数据卷。详见 `docs/40_移动审批实现与联调.md`。

### OA 免密配置补充

`CCR_MOBILE_OA_ENABLED` 默认 false；部署侧配置 `CCR_MOBILE_OA_VERIFY_URL`（固定地址含 `{ticket}`）、`CCR_MOBILE_OA_APP_ID`、`CCR_MOBILE_OA_API_KEY` 后启用。生产与测试 Compose 均已透传。OA 和有度两套认证并存；前者以 `/mobile/?ticket=...` 携票进入，后端 `/mobile/oa/login` 验证后建立移动审批会话。参数与接口约束见 `docs/40_移动审批实现与联调.md` 的 OA 补充章节。本次没有增加端口、数据表或数据卷。

## 2026-09-09 Authing code 免密登录

新增 `CCR_INTEGRATION_AUTH_CODE_ENABLED`（默认 false）、`CODE_TOKEN_URL`、`USER_INFO_URL`（默认空）、`CODE_TOKEN_PATH`（data）、`USER_NAME_PATH`（data.userBasicInfo.username）、`RESPONSE_CODE_PATH`（code）、`RESPONSE_SUCCESS_CODE`（200）；后六项均使用 `CCR_INTEGRATION_AUTH_` 前缀。两套 Compose 显式透传，复用原 Authing 应用标识、API Key 与请求头。完整环境变量及网关契约见 [41_Authing_code单点登录.md](41_Authing_code单点登录.md)。`./dev frontend-test` 新增电脑端认证行为回归，再执行原两端构建及移动回归；无新增端口、表结构或数据卷，不运行 reset。

Authing 网关契约补充：code 兑换使用 GET 查询参数，用户信息使用 POST JSON；两步复用密码认证的 `CCR_INTEGRATION_AUTH_APP_CODE`。已有环境文件若覆盖了旧响应路径，需改为 `CODE_TOKEN_PATH=data`、`USER_NAME_PATH=data.userBasicInfo.username`（均加 `CCR_INTEGRATION_AUTH_` 前缀），或移除覆盖值以使用新默认值。

## 2026-09-09 企业微信节点提醒

新增 `CCR_INTEGRATION_WECHAT_ENABLED`（默认 false）、`CCR_INTEGRATION_WECHAT_URL`（完整发送 URL，默认空），两套 Compose 均透传，复用已有 `CCR_INTEGRATION_AUTH_APP_ID/API_KEY` 与请求头配置。节点 Outbox 与通知队列每 30 秒扫描；企业微信失败最少间隔 2 分钟，处理中断租约 5 分钟，默认最多 3 次总尝试。沿用 `./dev backend-test`、`./dev app-up`、`./dev smoke` 验证，隔离栈固定关闭真实企微发送。本次无 DDL、新端口或新容器，不运行 reset、不重建数据卷。完整部署与验证见 [42_企业微信节点提醒.md](42_企业微信节点提醒.md)。

两项能力的合并部署模板位于 [deployment/README.md](deployment/README.md)：提供 `authing-wechat.env.example` 及 `docker-compose.authing-wechat.yml`，涵盖 16 项已有变量。示例使用占位网关和凭证；生产模板启用 code 登录与企微提醒，隔离测试须将企微开关设为 false。先合并现有 `.env` 和 backend.environment，再执行 `docker compose --env-file .env config --quiet` 校验；重建容器时需确保镜像或持久挂载中的 Jar 包含最新代码，原 `docker cp` 临时替换需按发布流程重新部署。
