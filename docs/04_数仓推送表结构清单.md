# 客户贡献度与利率决策系统 · 数仓推送表结构清单(提交数仓加工)

> 依据:《ccrd_dw_schema _修订.sql》(v1.1 / PRD V2 D1-D21)
> 用途:**申请时按客户姓名模糊查询带出客户信息**,以及审批所需的融资/担保/贡献度/征信/机构达成数据。本清单供数仓加工落地。
> 交付方:客户贡献度与利率决策系统(业务系统)
> 版本: v1.1 / 2026-08-07(v1.1 补集团客户数仓数据集 9 张表 T11-T19,依据 db/02_external_data.sql 集团段)
>
> **⚠️ 状态(2026-08-11)**:本文档已被 **`docs/09_数仓加工表清单.md`(数仓加工表设计文档 / 契约基准)取代**。全量 **20 张**表清单、统一编号与待确认项以 docs/09 为准;本文档降级为**逐表 DDL 底稿**(下表 T 编号为历史编号,附录与正文存在重复编号,请勿作为全量依据)。机构维度 `dw_org_dim` 已决策**不纳入数仓契约**(机构维度由系统内 `ccr_sys_dept`+`sys_org` 维护),本文档不涉及。

---

## 〇、总览(共 10 张推送表)

| # | 表名 | 说明 | 用途 | 时效 |
|---|---|---|---|---|
| T01 | `caps_corp_cust_basic_info` | 对公客户主数据 | 申请按客户姓名模糊查询带出 | 每日 |
| T02 | `caps_indv_cust_basic_info` | 对私客户主数据 | 申请按客户姓名模糊查询带出 | 每日 |
| T03 | ~~`dw_own_financing_snapshot`~~ | 本行融资合同 | ~~存量授信/原利率来源~~ **已并入贷款合同 B07(2026-08-11 去冗余)** | — |
| T04 | `dw_mortgage_snapshot` | 抵押物 | 担保展示/补充 | 每日 |
| T05 | `dw_guarantor_snapshot` | 担保人 | 担保展示/补充 | 每日 |
| T06 | `dw_contribution_metric` | 当前贡献度(单指标,勾稽) | 贡献度卡片/承诺跟踪 | T+1 |
| T07 | `dw_credit_report_snapshot` | 征信报告头 | 征信展示 | 无时效 |
| T08 | `dw_credit_financing_detail` | 征信他行融资明细 | 他行融资 | 无时效 |
| T09 | `dw_credit_financing_summary` | 他行融资概要 | 他行融资概要 | 无时效 |
| T10 | `dw_org_performance_snapshot` | 机构达成 | 机构达成率 | 月度 |

**通用约定**:字符集 utf8mb4;金额 `DECIMAL(18,4)`(万元);利率 `DECIMAL(7,4)`(百分值,如 3.8500=3.85%);敏感字段(证件号/存款账号)**明文存储**(2026-08-17 明文化);每表以 `etl_md5` 为主键 + `data_dt` 标识批次,业务读取最新 `data_dt` 批次。

---

## T01 `caps_corp_cust_basic_info` 对公客户主数据 ⭐(姓名模糊查询核心)

```sql
CREATE TABLE caps_corp_cust_basic_info (
  etl_md5           CHAR(32)     NOT NULL COMMENT '源系统ETL_MD5(主键)',
  data_dt           DATE         NOT NULL COMMENT '数据日期',
  cust_no           VARCHAR(32)  NOT NULL COMMENT '客户号',
  cust_name         VARCHAR(128) NOT NULL COMMENT '客户名称',
  cert_tp           VARCHAR(10)  NOT NULL COMMENT '证件类型',
  cert_no           VARCHAR(64)  NOT NULL COMMENT '统一社会信用代码(明文)',
  ffthlv_class      VARCHAR(10)  NULL COMMENT '客户五级分类',
  entp_charic       VARCHAR(20)  NULL COMMENT '企业性质(国企/非国企)',
  entp_scale        VARCHAR(20)  NULL COMMENT '企业规模',
  blgd_idsty        VARCHAR(20)  NULL COMMENT '所属行业',
  crdt_grd          VARCHAR(10)  NULL COMMENT '内部信用等级',
  entp_empe_num     INT          NULL COMMENT '员工人数',
  rest_addr         VARCHAR(256) NULL COMMENT '注册地址',
  rest_asts         DECIMAL(18,4) NULL COMMENT '注册资本(万元)',
  estp_estb_dt      DATE         NULL COMMENT '成立日期',
  openact_org_no    VARCHAR(32)  NULL COMMENT '开户机构编号',
  openact_org_nm    VARCHAR(32)  NULL COMMENT '开户机构名称',
  openact_dt        DATE         NULL COMMENT '开户日期',
  cust_class        VARCHAR(10)  NOT NULL COMMENT '客户性质:EXISTING存客/NEW新客',
  PRIMARY KEY (etl_md5),
  KEY idx_cust_no (cust_no),
  KEY idx_cert_no (cert_no),
  KEY idx_cust_name (cust_name)          -- ⭐ 客户姓名模糊查询索引
);
```

## T02 `caps_indv_cust_basic_info` 对私客户主数据 ⭐(姓名模糊查询核心)

```sql
CREATE TABLE caps_indv_cust_basic_info (
  etl_md5           CHAR(32)     NOT NULL COMMENT '源系统ETL_MD5(主键)',
  data_dt           DATE         NOT NULL COMMENT '数据日期',
  cust_no           VARCHAR(32)  NOT NULL COMMENT '客户号',
  cust_nm           VARCHAR(128) NOT NULL COMMENT '客户姓名',
  cert_tp           VARCHAR(10)  NOT NULL COMMENT '证件类型',
  cert_no           VARCHAR(64)  NOT NULL COMMENT '证件号(明文)',
  gnd               CHAR(64)     NOT NULL COMMENT '性别',
  ffthlv_class      VARCHAR(10)  NULL COMMENT '五级分类',
  ocupn             VARCHAR(40)  NULL COMMENT '职业',
  whlyr_incm        DECIMAL(18,4) NULL COMMENT '全年收入(万元)',
  mrrg_sittn        VARCHAR(10)  NULL COMMENT '婚姻状况',
  rsd_addr          VARCHAR(256) NULL COMMENT '居住地址',
  tel_no            VARCHAR(32)  NULL COMMENT '联系电话',
  opnact_org_no     VARCHAR(32)  NULL COMMENT '开户机构编号',
  opnact_org_nm     VARCHAR(32)  NULL COMMENT '开户机构名称',
  opnact_dt         DATE         NULL COMMENT '开户日期',
  cust_class        VARCHAR(10)  NOT NULL COMMENT '客户性质:EXISTING存客/NEW新客',
  PRIMARY KEY (etl_md5),
  KEY idx_cust_no (cust_no),
  KEY idx_cert_no (cert_no),
  KEY idx_cust_nm (cust_nm)              -- ⭐ 客户姓名模糊查询索引
);
```

> **申请带出逻辑**:客户经理输入客户姓名 → `LIKE '%姓名%'` 模糊查询 → 带出客户号、证件、客户性质、开户机构、五级分类/信用等级/企业性质/注册资本等,作为申请客户信息(只读展示)。

## T03 ~~`dw_own_financing_snapshot`~~ 本行融资合同（⚠️ 已并入贷款合同 B07,2026-08-11 去冗余,不再推送;契约以 docs/09 为准）

```sql
CREATE TABLE dw_own_financing_snapshot (
  etl_md5           BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt           DATE    NOT NULL,
  cust_no           VARCHAR(32) NOT NULL COMMENT '客户号',
  contract_no       VARCHAR(32) NOT NULL COMMENT '授信合同号',
  crgant_start_dt   VARCHAR(32) NULL COMMENT '授信开始日期',
  crgant_due_dt     VARCHAR(32) NULL COMMENT '授信到期日期',
  crgant_matu       INT     NOT NULL COMMENT '授信期限',
  guarantee_type    VARCHAR(10) NOT NULL COMMENT '担保类型',
  loan_balance      DECIMAL(18,4) NOT NULL COMMENT '贷款余额(万元)',
  contract_rate     DECIMAL(7,4)  NOT NULL COMMENT '原利率%(本行合同当前利率)',
  PRIMARY KEY (etl_md5),
  KEY idx_cust (cust_no),
  KEY idx_contract (contract_no)
);
```
> 存量授信申请按此表合同切分额度;`contract_rate` 作为申请原利率。

## T04 `dw_mortgage_snapshot` 抵押物

```sql
CREATE TABLE dw_mortgage_snapshot (
  etl_md5           BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt           DATE    NOT NULL,
  cust_no           VARCHAR(32) NOT NULL COMMENT '被担保客户号',
  contract_no       VARCHAR(32) NULL COMMENT '关联授信合同号',
  mortgage_type     VARCHAR(20) NOT NULL COMMENT '抵押物类型:HOUSE/LAND/EQUIPMENT/VEHICLE/OTHERS',
  mortgage_name     VARCHAR(128) NULL,
  owner_name        VARCHAR(128) NULL COMMENT '权属人名称',
  owner_cert_no     VARCHAR(512) NULL COMMENT '权属人证件号',
  register_no       VARCHAR(64)  NULL COMMENT '登记/他项权证编号',
  assess_value      DECIMAL(18,4) NULL COMMENT '评估价值(万元)',
  assess_date       DATE         NULL,
  mortgage_ratio    DECIMAL(7,4)  NULL COMMENT '抵押率%',
  mortgage_addr     VARCHAR(128) NULL,
  PRIMARY KEY (etl_md5),
  KEY idx_cust (cust_no),
  KEY idx_contract (contract_no)
);
```

## T05 `dw_guarantor_snapshot` 担保人

```sql
CREATE TABLE dw_guarantor_snapshot (
  etl_md5           BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt           DATE    NOT NULL,
  cust_no           VARCHAR(32) NOT NULL COMMENT '被担保客户号',
  contract_no       VARCHAR(32) NULL COMMENT '关联授信合同号',
  guarantor_name    VARCHAR(128) NOT NULL COMMENT '担保人名称',
  guarantor_cert_type VARCHAR(10) NULL,
  guarantor_cert_no VARCHAR(512) NULL COMMENT '担保人证件号',
  guarantee_type    VARCHAR(20) NULL COMMENT '担保方式:GENERAL/JOINT/OTHER',
  guarantee_amount  DECIMAL(18,4) NULL COMMENT '担保金额(万元)',
  guarantee_balance DECIMAL(18,4) NULL COMMENT '担保余额(万元)',
  PRIMARY KEY (etl_md5),
  KEY idx_cust (cust_no),
  KEY idx_contract (contract_no)
);
```

## T06 `dw_contribution_metric` 当前贡献度(单指标,表内勾稽)

```sql
CREATE TABLE dw_contribution_metric (
  etl_md5           BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt           DATE    NOT NULL,
  cust_no           VARCHAR(32) NOT NULL,
  metric_code       VARCHAR(40) NOT NULL COMMENT '指标编码;TOTAL=综合贡献总额行,其余为明细(对公9项/对私/关联)',
  metric_name       VARCHAR(64) NOT NULL,
  metric_value      DECIMAL(18,4) NOT NULL COMMENT '指标值(万元);TOTAL行=综合贡献总额',
  value_type        VARCHAR(20) NOT NULL COMMENT 'AVG_BALANCE业务余额/INCOME收入/CONTRIBUTION_AMOUNT折算',
  metric_scope      VARCHAR(15) NOT NULL COMMENT 'PUBLIC/PRIVATE_SELF/RELATED',
  PRIMARY KEY (etl_md5),
  KEY idx_cust (cust_no),
  KEY idx_cust_code (cust_no, metric_code)
);
```
> 指标清单(PRD §9.0):对公 `PUBLIC_DEPOSIT_AVG/PROJECT_LOAN_AVG/DISCOUNT_SPREAD/OFF_BALANCE_INCOME/EXCHANGE_SPREAD/PAYROLL_CONTRIBUTION/PAYROLL_AMOUNT/WEALTH_INCOME`;对私 `PRIVATE_DEPOSIT_AVG/LOAN_AVG/WEALTH_INCOME`;派生参考 `PUBLIC_DEPOSIT_LOAN_RATIO`(RATIO,不参与勾稽)。TOTAL 行与明细行须勾稽一致。

## T07–T09 征信(无时效限制)

```sql
-- 征信报告头
CREATE TABLE dw_credit_report_snapshot (
  etl_md5           BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt           DATE    NOT NULL,
  cust_no           VARCHAR(32) NOT NULL,
  report_date       DATE         NULL,
  parse_status      VARCHAR(10) NOT NULL COMMENT 'COMPLETE/PARTIAL',
  PRIMARY KEY (etl_md5),
  KEY idx_cust (cust_no)
);
-- 他行融资明细
CREATE TABLE dw_credit_financing_detail (
  etl_md5           BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt           DATE    NOT NULL,
  report_id         BIGINT  NOT NULL COMMENT '关联征信头id',
  customer_no       VARCHAR(32) NOT NULL,
  lender_name       VARCHAR(64) NOT NULL COMMENT '他行机构名',
  credit_amount     DECIMAL(18,4) NULL COMMENT '授信额(万元)',
  used_amount       DECIMAL(18,4) NULL,
  balance_amount    DECIMAL(18,4) NULL,
  annual_rate       DECIMAL(7,4)  NULL COMMENT '年化利率%',
  PRIMARY KEY (etl_md5),
  KEY idx_report (report_id),
  KEY idx_cust (customer_no)
);
-- 他行融资概要
CREATE TABLE dw_credit_financing_summary (
  etl_md5            BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt            DATE    NOT NULL,
  report_id          BIGINT  NOT NULL,
  cust_no            VARCHAR(32) NOT NULL,
  lender_count       INT     NOT NULL COMMENT '授信机构数',
  npl_balance        DECIMAL(18,4) NULL COMMENT '不良贷款余额(万元)',
  credit_amount_total DECIMAL(18,4) NULL COMMENT '他行授信总额',
  used_amount_total  DECIMAL(18,4) NULL COMMENT '他行已用额度合计',
  loan_account_count INT     NULL COMMENT '未结清贷款笔数',
  overdue_account_count INT  NULL COMMENT '逾期账户数',
  overdue_balance    DECIMAL(18,4) NULL COMMENT '逾期贷款余额',
  special_mention_balance DECIMAL(18,4) NULL COMMENT '关注类余额',
  external_guarantee_balance DECIMAL(18,4) NULL COMMENT '对外担保余额',
  PRIMARY KEY (etl_md5),
  KEY idx_report (report_id),
  KEY idx_cust (cust_no)
);
```

## T10 `dw_org_performance_snapshot` 机构达成

```sql
CREATE TABLE dw_org_performance_snapshot (
  etl_md5           BIGINT  NOT NULL AUTO_INCREMENT,
  data_dt           DATE    NOT NULL,
  org_code          VARCHAR(32) NOT NULL COMMENT '申报机构编码',
  stat_month        CHAR(6) NOT NULL COMMENT '统计月份YYYYMM',
  achieved_amount   DECIMAL(18,4) NOT NULL COMMENT '已达成贡献金额(万元)',
  expected_amount   DECIMAL(18,4) NOT NULL COMMENT '应达成贡献金额(=已通过申请拟达成合计)',
  completion_rate   DECIMAL(7,4) NOT NULL COMMENT '达成率=已达成/应达成',
  snapshot_ts       DATETIME NOT NULL,
  PRIMARY KEY (etl_md5),
  KEY idx_org_month (org_code, stat_month)
);
```

---

## 集团客户数仓数据集(8 张,V1.0 A.5)

> 依据:`db/02_external_data.sql` 集团段(dw_*_snapshot)。集团功能数据源,层级:**集团-成员-集团授信-成员额度-贷款合同-借据**(2026-08-17 去「用信分项」中间层)。
> 与 T01-T10 同一通用约定(`etl_md5` 主键 + `data_dt` 批次,业务读取最新 `data_dt` 批次);本组表随集团申请/审批场景推送。

| # | 表名 | 说明 | 关联层级 |
|---|---|---|---|
| T11 | `dw_customer_group_snapshot` | 集团客户主数据快照 | 集团 |
| T12 | `dw_customer_group_member_snapshot` | 集团成员快照(核心/一般) | 成员 |
| T13 | `dw_customer_relation_snapshot` | 客户关系快照(集团外关联) | 关联 |
| T14 | `dw_group_credit_snapshot` | 集团授信快照 | 集团授信 |
| T15 | `dw_member_credit_limit_snapshot` | 成员额度快照(独占/共享) | 成员额度 |
| T16 | `dw_loan_contract_snapshot` | 贷款合同快照(决议核验利率源) | 合同 |
| T18 | `dw_loan_note_snapshot` | 借据快照(决议核验比对合同利率) | 借据 |
| T19 | `dw_deposit_account_snapshot` | 存款账户快照(账号反查,明文匹配) | 账户 |

## T11 `dw_customer_group_snapshot` 集团客户主数据快照

```sql
CREATE TABLE dw_customer_group_snapshot (
  etl_md5           BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt           DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  group_no          VARCHAR(64)  NOT NULL COMMENT '集团客户编号',
  group_name        VARCHAR(128) NOT NULL COMMENT '集团名称',
  group_type        VARCHAR(20)  NOT NULL COMMENT '集团类型',
  manager_org_id    VARCHAR(32)  NULL COMMENT '集团主办机构编号',
  group_status      VARCHAR(16)  NOT NULL COMMENT '集团状态:NORMAL正常/DISSOLVED解散',
  PRIMARY KEY (etl_md5),
  KEY idx_group_no (group_no)
);
```

## T12 `dw_customer_group_member_snapshot` 集团成员快照

```sql
CREATE TABLE dw_customer_group_member_snapshot (
  etl_md5            BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt            DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  group_no           VARCHAR(64)  NOT NULL COMMENT '集团客户编号',
  member_customer_no VARCHAR(64)  NOT NULL COMMENT '成员客户号',
  member_role        VARCHAR(20)  NULL COMMENT '成员角色:CORE核心/GENERAL一般',
  control_relation   VARCHAR(32)  NULL COMMENT '控制关系:控股/参股等',
  is_core_member     CHAR(1)      NOT NULL DEFAULT 'N' COMMENT '是否核心成员Y/N',
  relation_start     DATE         NULL COMMENT '入团日期',
  relation_end       DATE         NULL COMMENT '退团日期(空=在团)',
  PRIMARY KEY (etl_md5),
  KEY idx_gm_group (group_no),
  KEY idx_gm_member (member_customer_no)
);
```

## T13 `dw_customer_relation_snapshot` 客户关系快照(集团外关联关系)

```sql
CREATE TABLE dw_customer_relation_snapshot (
  etl_md5              BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt              DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  customer_no          VARCHAR(64)  NOT NULL COMMENT '客户号',
  related_customer_no  VARCHAR(64)  NOT NULL COMMENT '关联方客户号',
  relation_type        VARCHAR(32)  NOT NULL COMMENT '关系类型:GROUP_MEMBER/GUARANTEE/INVEST等',
  relation_strength    VARCHAR(16)  NULL COMMENT '关系强度:STRONG/WEAK',
  relation_start       DATE         NULL COMMENT '关系开始日期',
  relation_end         DATE         NULL COMMENT '关系结束日期',
  relation_status      VARCHAR(16)  NOT NULL COMMENT 'VALID有效/INVALID失效',
  PRIMARY KEY (etl_md5),
  KEY idx_rel_cust (customer_no),
  KEY idx_rel_related (related_customer_no)
);
```

## T14 `dw_group_credit_snapshot` 集团授信快照

```sql
CREATE TABLE dw_group_credit_snapshot (
  etl_md5                 BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt                 DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  group_credit_no         VARCHAR(64)  NOT NULL COMMENT '集团授信编号',
  group_no                VARCHAR(64)  NOT NULL COMMENT '集团客户编号',
  approved_total_amount   DECIMAL(18,4) NOT NULL COMMENT '批复总额度(万元)',
  allocated_amount        DECIMAL(18,4) NOT NULL COMMENT '已分配额度(万元)',
  used_amount             DECIMAL(18,4) NOT NULL COMMENT '已用额度(万元)',
  available_amount        DECIMAL(18,4) NOT NULL COMMENT '可用额度(万元)',
  currency                VARCHAR(8)   NOT NULL DEFAULT 'CNY' COMMENT '币种',
  credit_start            DATE         NOT NULL COMMENT '授信开始日期',
  credit_end              DATE         NOT NULL COMMENT '授信到期日期',
  revolving_flag          CHAR(1)      NOT NULL DEFAULT 'N' COMMENT '是否循环额度Y/N',
  credit_status           VARCHAR(16)  NOT NULL COMMENT 'EFFECTIVE有效/EXPIRED到期/FROZEN冻结',
  PRIMARY KEY (etl_md5),
  KEY idx_gc_group (group_no),
  KEY idx_gc_no (group_credit_no)
);
```

## T15 `dw_member_credit_limit_snapshot` 成员额度快照

```sql
CREATE TABLE dw_member_credit_limit_snapshot (
  etl_md5               BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt               DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  member_limit_no       VARCHAR(64)  NOT NULL COMMENT '成员额度编号',
  group_credit_no       VARCHAR(64)  NOT NULL COMMENT '所属集团授信编号',
  member_customer_no    VARCHAR(64)  NOT NULL COMMENT '成员客户号',
  allocation_mode       VARCHAR(16)  NOT NULL COMMENT 'EXCLUSIVE独占分配/SHARED共享',
  allocated_amount      DECIMAL(18,4) NOT NULL COMMENT '分配额度(万元)',
  used_amount           DECIMAL(18,4) NOT NULL COMMENT '已用额度(万元)',
  available_amount      DECIMAL(18,4) NOT NULL COMMENT '可用额度(万元)',
  shared_limit_group_no VARCHAR(64)  NULL COMMENT '共享额度组编号(SHARED场景)',
  currency              VARCHAR(8)   NOT NULL DEFAULT 'CNY' COMMENT '币种',
  limit_start           DATE         NOT NULL COMMENT '额度开始日期',
  limit_end             DATE         NOT NULL COMMENT '额度到期日期',
  limit_status          VARCHAR(16)  NOT NULL COMMENT 'EFFECTIVE有效/EXPIRED到期/FROZEN冻结',
  PRIMARY KEY (etl_md5),
  KEY idx_ml_no (member_limit_no),
  KEY idx_ml_gc (group_credit_no),
  KEY idx_ml_member (member_customer_no)
);
```

## T16 `dw_loan_contract_snapshot` 贷款合同快照

```sql
CREATE TABLE dw_loan_contract_snapshot (
  etl_md5            BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt            DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  contract_no        VARCHAR(64)  NOT NULL COMMENT '贷款合同号',
  tranche_no         VARCHAR(64)  NULL COMMENT '存量不填(集团切分授信无分项层,保留列兼容数仓旧推送)',
  borrower_customer_no VARCHAR(64) NOT NULL COMMENT '借款人客户号',
  contract_amount    DECIMAL(18,4) NOT NULL COMMENT '合同金额(万元)',
  contract_balance   DECIMAL(18,4) NOT NULL COMMENT '合同余额(万元)',
  currency           VARCHAR(8)   NOT NULL DEFAULT 'CNY' COMMENT '币种',
  execution_rate     DECIMAL(7,4) NOT NULL COMMENT '合同执行利率%',
  rate_type          VARCHAR(16)  NOT NULL COMMENT 'FIXED固定/LPR_PLUS LPR加点',
  lpr_term           VARCHAR(8)   NULL COMMENT 'LPR期限:1Y/5Y(LPR加点时必填)',
  start_date         DATE         NOT NULL COMMENT '合同起始日',
  maturity_date      DATE         NOT NULL COMMENT '合同到期日',
  contract_status    VARCHAR(16)  NOT NULL COMMENT 'EFFECTIVE有效/SETTLED结清/OVERDUE逾期',
  contract_version   INT          NOT NULL DEFAULT 1 COMMENT '合同版本(补充协议递增)',
  PRIMARY KEY (etl_md5),
  KEY idx_lc_no (contract_no),
  KEY idx_lc_borrower (borrower_customer_no)
);
```

## T18 `dw_loan_note_snapshot` 借据快照

```sql
CREATE TABLE dw_loan_note_snapshot (
  etl_md5             BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt             DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  loan_note_no        VARCHAR(64)  NOT NULL COMMENT '借据号',
  contract_no         VARCHAR(64)  NOT NULL COMMENT '所属贷款合同号',
  tranche_no          VARCHAR(64)  NULL COMMENT '存量不填(集团切分授信无分项层,保留列兼容数仓旧推送)',
  borrower_customer_no VARCHAR(64) NOT NULL COMMENT '借款人客户号',
  loan_amount         DECIMAL(18,4) NOT NULL COMMENT '借据金额(万元)',
  loan_balance        DECIMAL(18,4) NOT NULL COMMENT '借据余额(万元)',
  currency            VARCHAR(8)   NOT NULL DEFAULT 'CNY' COMMENT '币种',
  execution_rate      DECIMAL(7,4) NOT NULL COMMENT '借据执行利率%',
  rate_type           VARCHAR(16)  NOT NULL COMMENT 'FIXED固定/LPR_PLUS LPR加点',
  lpr_term            VARCHAR(8)   NULL COMMENT 'LPR期限:1Y/5Y',
  start_date          DATE         NOT NULL COMMENT '放款日期',
  maturity_date       DATE         NOT NULL COMMENT '借据到期日',
  note_status         VARCHAR(16)  NOT NULL COMMENT 'NORMAL正常/SETTLED结清/OVERDUE逾期',
  PRIMARY KEY (etl_md5),
  KEY idx_ln_no (loan_note_no),
  KEY idx_ln_contract (contract_no),
  KEY idx_ln_borrower (borrower_customer_no)
);
```

## T19 `dw_deposit_account_snapshot` 存款账户快照

```sql
CREATE TABLE dw_deposit_account_snapshot (
  etl_md5                  BIGINT       NOT NULL AUTO_INCREMENT,
  data_dt                  DATE         NOT NULL COMMENT '数据日期(DATA_DT)',
  deposit_account_no       VARCHAR(64)  NOT NULL COMMENT '存款账号(明文)',
  customer_no              VARCHAR(64)  NOT NULL COMMENT '客户号',
  product_code             VARCHAR(32)  NOT NULL COMMENT '存款产品编码',
  account_balance          DECIMAL(18,4) NOT NULL COMMENT '账户余额(万元)',
  currency                 VARCHAR(8)   NOT NULL DEFAULT 'CNY' COMMENT '币种',
  execution_rate           DECIMAL(7,4) NOT NULL COMMENT '执行利率%',
  rate_type                VARCHAR(16)  NOT NULL COMMENT 'FIXED固定/FLOAT浮动',
  term_value               INT          NULL COMMENT '期限数值(活期为空)',
  term_unit                VARCHAR(8)   NULL COMMENT '期限单位:日/月/年',
  open_date                DATE         NOT NULL COMMENT '开户日期',
  maturity_date            DATE         NULL COMMENT '到期日期(活期为空)',
  account_status           VARCHAR(16)  NOT NULL COMMENT 'NORMAL正常/CLOSED销户/FROZEN冻结',
  PRIMARY KEY (etl_md5),
  KEY idx_da_no (deposit_account_no),
  KEY idx_da_cust (customer_no)
);
```

> **关联人说明**:关联人无独立关系表;关联人身份由客户经理申请时录入(证件号),数仓按其证件号反查关联人贡献度明细,承载在 `dw_contribution_metric.metric_scope='RELATED'` 行(PRD V2 §8.2)。

---

## 附:提交前必做校验(PRD §8.4)

1. 客户数据存在且证件匹配唯一。
2. 存量客户本行融资合同存在,原利率与合同利率一致。
3. 当前贡献度未超时效、明细完整且勾稽通过。
4. 主客户、关联人当前贡献度统计区间一致。
5. 征信解析完整(无时效限制)。
6. 仓库数据存在阻断级质量问题时阻止提交。

## 待数仓确认项

1. 客户姓名模糊查询对超大表(百万级)的索引与性能;是否需要按机构/客户号预过滤。
2. `dw_contribution_metric` 的 TOTAL 行由数仓折算提供,系统对明细加总勾稽。
3. 各表批次频率(T+1)与成功事件推送方式(业务系统批次监听)。
4. **批次幂等 UK(待补)**:本清单 DDL 以 `etl_md5` 为主键 + `data_dt` 标识批次,但业务主键列(如 group_no/member_customer_no/contract_no 等)未建批次级唯一约束;数仓落地时建议补 `(data_dt, 业务主键)` 幂等唯一键,防止同批次重复加载(对应 docs/05 §9.4.2 数仓落地表补批次号幂等列)。具体列以各表业务主键为准,待数仓侧确认后回填至本清单。

## 附：2026-08 联调新增推送表（DDL 见 db/02_external_data.sql）

| 编号 | 表名 | 说明 | 频率 |
|---|---|---|---|
| T11 | `dw_customer_group_snapshot` | 集团主数据 | 每日 |
| T12 | `dw_customer_group_member_snapshot` | 集团成员关系 | 每日 |
| T13 | `dw_customer_relation_snapshot` | 客户关系/关联人 | 每日 |
| T14 | `dw_group_credit_snapshot` | 集团综合授信 | 每日 |
| T15 | `dw_member_credit_limit_snapshot` | 成员额度拆分 | 每日 |
| T16 | `dw_loan_contract_snapshot` | 贷款合同 | 每日 |
| T18 | `dw_loan_note_snapshot` | 借据（核验用） | 每日 |
| T19 | `dw_deposit_account_snapshot` | 存款账户（明文） | 每日 |
| T20 | `dw_credit_agreement_snapshot` | 授信协议 | 每日 |
| 变更 | `dw_mortgage_snapshot` | 增加 ext_json 类型特有元素列 | 每日 |
